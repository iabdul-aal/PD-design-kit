
\chapter{Germanium-on-Silicon PIN Photodetector}
\label{ch:photodetector}

The detection of optical signals at \SI{1310}{\nano\metre} constitutes the
first irreversible transduction step in any silicon-photonic receiver chain:
the continuous optical field is collapsed into a discrete electrical current,
and whatever information is lost at this stage propagates as an irrecoverable
noise penalty into every subsequent digital signal processing block.
Understanding this step at the level of first principles is therefore not merely
a theoretical exercise but a practical necessity for the coherent design of the
full transceiver system developed in the companion chapters of this thesis.

This chapter develops that understanding from the ground up.
Section~\ref{sec:pd_concept} constructs the analytical framework rigorously,
beginning with Maxwell's equations in a dispersive absorbing medium, from which
the absorption coefficient of germanium emerges as a derived quantity; carrier
transport, recombination, and the full self-consistent drift-diffusion system are
then formalised in both their transient and steady-state forms, and all performance
metrics are defined with sufficient precision to serve as unambiguous comparison
axes.
Section~\ref{sec:pd_literature} begins with a staged device-evolution argument,
then applies those metrics to a critical review of waveguide-integrated
photodetectors, and closes with a comparative summary that positions the
present O-band Ge-on-Si PIN within the state of the art.
Section~\ref{sec:pd_design} presents the device architecture, the benchmark
anchoring strategy, and the coupled optical-electrical simulation methodology
used to connect the FDTD, CHARGE, and reduced analytical models.
Section~\ref{sec:pd_results} reports the main simulation results and interprets
the dominant trade-offs that emerge from them.
Section~\ref{sec:pd_system} derives, strictly from the extracted device metrics,
the implications for receiver sensitivity and modulation-format compatibility,
culminating in the extraction of device-level specifications from the requirements
of the IEEE~802.3 400GBASE-DR4 standard. Section~\ref{sec:pd_pinn} then uses a
validated PINN surrogate to extend the single-U benchmark toward the double-U
design space. Section~\ref{sec:pd_conclusion} distils the principal physical
insights and identifies open questions that future experimental work must address.

\section{Physical Foundations and Metric Formalisation}
\label{sec:pd_concept}

\subsection{Electromagnetic Propagation in an Absorbing Dispersive Medium}
\label{subsec:pd_maxwell}

The starting point for all optical analysis in this chapter is the macroscopic
form of Maxwell's equations in a non-magnetic, source-free medium. In SI units
and in the frequency domain, obtained by assuming a time dependence
$e^{-i\omega t}$, these read:
\begin{align}
    \nabla \times \tilde{\mathbf{E}}(\mathbf{r},\omega)
        &= i\omega\mu_0\,\tilde{\mathbf{H}}(\mathbf{r},\omega),
        \label{eq:maxwell_curl_e}\\
    \nabla \times \tilde{\mathbf{H}}(\mathbf{r},\omega)
        &= -i\omega\tilde{\varepsilon}(\omega)\,\tilde{\mathbf{E}}(\mathbf{r},\omega),
        \label{eq:maxwell_curl_h}
\end{align}
where $\mu_0$ is the permeability of free space and $\tilde{\varepsilon}(\omega)$
is the complex permittivity. Taking the curl of \cref{eq:maxwell_curl_e} and
substituting \cref{eq:maxwell_curl_h} yields the Helmholtz wave equation:
\begin{equation}
    \nabla^2\tilde{\mathbf{E}}(\mathbf{r},\omega)
    + \tilde{k}^2(\omega)\,\tilde{\mathbf{E}}(\mathbf{r},\omega) = \mathbf{0},
    \label{eq:helmholtz}
\end{equation}
where $\tilde{k}(\omega) = \omega\sqrt{\mu_0\tilde{\varepsilon}(\omega)}$.
Defining the \emph{complex refractive index}:
\begin{equation}
    \tilde{n}(\omega) \equiv \frac{c\,\tilde{k}(\omega)}{\omega}
    = n(\omega) + i\kappa(\omega),
    \label{eq:complex_n}
\end{equation}
the plane-wave solution propagating along $+\hat{z}$ is:
\begin{equation}
    \tilde{\mathbf{E}}(z,\omega)
    = \mathbf{E}_0
      \exp\!\Bigl[i\,n(\omega)\frac{\omega z}{c}\Bigr]
      \exp\!\Bigl[-\kappa(\omega)\frac{\omega z}{c}\Bigr].
    \label{eq:plane_wave}
\end{equation}
The time-averaged Poynting intensity $I(z)\propto|\tilde{\mathbf{E}}|^2$ decays
as $\exp[-2\kappa\omega z/c]$, giving the Beer-Lambert absorption coefficient:
\begin{equation}
    \alpha(\omega) = \frac{2\omega\kappa(\omega)}{c}
    = \frac{4\pi\kappa(\lambda)}{\lambda},
    \label{eq:alpha_kappa}
\end{equation}
so that the Beer-Lambert law:
\begin{equation}
    I(z) = I_0\exp\!\bigl[-\alpha(\lambda)\,z\bigr]
    \label{eq:beer_lambert}
\end{equation}
emerges as a corollary of \cref{eq:helmholtz} rather than an independent
empirical postulate. The absorbed power density follows directly:
\begin{equation}
    \mathcal{P}_{\mathrm{abs}}(\mathbf{r})
    = \alpha(\lambda)\,I(\mathbf{r}).
    \label{eq:abs_density}
\end{equation}

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.75\linewidth]{
        Spectral dependence of $\kappa(\lambda)$ and
        $\alpha(\lambda)=4\pi\kappa/\lambda$ in bulk germanium across the
        O- and C-bands, derived from \cref{eq:alpha_kappa}. The direct-gap
        absorption edge near \SI{1550}{\nano\metre} and the large
        $\alpha\approx\SI{5e3}{\per\centi\metre}$ at \SI{1310}{\nano\metre}
        (shaded marker) should be visible. Data source: Palik (1985).}
    \caption{Spectral absorption coefficient $\alpha(\lambda)$ and extinction
    coefficient $\kappa(\lambda)$ of bulk germanium, derived via
    \cref{eq:alpha_kappa} from tabulated optical constants. The large $\alpha$
    at \SI{1310}{\nano\metre} reflects direct-gap absorption at the
    $\Gamma$-point discussed in \cref{subsec:pd_bandstructure}.}
    \label{fig:alpha_kappa}
\end{figure}

\paragraph{Poynting-vector formulation of the generation rate.}
The optical generation rate $G_{\mathrm{opt}}(\mathbf{r})$ defined below in
\cref{eq:gen_rate} is more generally obtained from the time-averaged divergence
of the Poynting vector. The divergence theorem applied to the time-averaged
Poynting flux $\langle\mathbf{S}\rangle = \tfrac{1}{2}
\mathrm{Re}[\tilde{\mathbf{E}}\times\tilde{\mathbf{H}}^{*}]$ over a closed
surface $\partial\Omega$ enclosing a volume $\Omega$ of absorbing material gives:
\begin{equation}
    \int_\Omega G_{\mathrm{opt}}(\mathbf{r})\,\hbar\omega\,\mathrm{d}V
    = -\oint_{\partial\Omega}
      \langle\mathbf{S}\rangle\cdot\hat{n}\,\mathrm{d}A,
    \label{eq:gopt_poynting}
\end{equation}
so that the local volumetric generation rate is the Poynting-flux sink:
\begin{equation}
    G_{\mathrm{opt}}(\mathbf{r})
    = -\frac{\nabla\cdot\langle\mathbf{S}(\mathbf{r})\rangle}{\hbar\omega}
    = \frac{\omega\varepsilon_0\,\mathrm{Im}[\tilde{\varepsilon}(\mathbf{r})]
           |\tilde{\mathbf{E}}(\mathbf{r})|^2}{2\,\hbar\omega}.
    \label{eq:gopt_poynting_local}
\end{equation}
This reduces identically to \cref{eq:gen_rate} under the plane-wave
approximation, but is the operationally correct definition when the
three-dimensional field contains interference terms arising from modal coupling,
metal reflections, or end-facet diffraction---all of which are present in the
adiabatic taper region of the device studied here.
The FDTD solver employed in \cref{subsec:pd_fdtd} evaluates
$G_{\mathrm{opt}}$ from \cref{eq:gopt_poynting_local}; the scalar Beer-Lambert
law of \cref{eq:beer_lambert} serves only as the analytical validation baseline.

\paragraph{Square-law photodetection and the domain of linear operation.}
A corollary of the plane-wave analysis concerns the fundamental nature of the
photodetection mechanism. For a quasi-monochromatic field at carrier frequency
$\omega_0$ with slowly varying complex envelope $\tilde{s}(t)$:
\begin{equation}
    \tilde{\mathbf{E}}(t)
    = \tfrac{1}{2}\tilde{s}(t)\,e^{-i\omega_0 t}\hat{\mathbf{e}} + \mathrm{c.c.},
    \label{eq:envelope_field}
\end{equation}
the time-averaged Poynting intensity at the detector facet is
$I(t)=|\tilde{s}(t)|^2/(2\eta_0)$, where $\eta_0=\sqrt{\mu_0/\varepsilon_0}$.
The photocurrent produced by the device is therefore:
\begin{equation}
    i_{\mathrm{ph}}(t)
    = \mathcal{R}\cdot P(t)
    = \mathcal{R}\cdot\frac{|\tilde{s}(t)|^2}{2\eta_0},
    \label{eq:square_law}
\end{equation}
which is \emph{linear in optical power} $P(t)$ but \emph{quadratic in optical
field amplitude} $|\tilde{s}(t)|$. When the field contains both a
data-bearing modulated component $\tilde{u}(t)$ and a residual DC carrier $c$,
$\tilde{s}(t)=\tilde{u}(t)+c$, the photocurrent expands as:
\begin{equation}
    i(t) = \mathcal{R}\bigl[
        \underbrace{c^2}_{\text{DC bias}}
       +\underbrace{2c\,\mathrm{Re}\{\tilde{u}(t)\}}_{\text{linear signal term}}
       +\underbrace{|\tilde{u}(t)|^2}_{\text{SSBI}}
    \bigr],
    \label{eq:square_law_expand}
\end{equation}
where SSBI denotes signal-signal beating interference. The linear approximation
$i_{\mathrm{ph}}\approx\mathcal{R}\cdot P_{\mathrm{opt}}$ is valid when the
carrier-to-signal power ratio $\mathrm{CSPR}\equiv c^2/\langle|\tilde{u}|^2\rangle\gg1$,
so that SSBI is negligible relative to the cross-beat term. For CW illumination
at $P_{\mathrm{opt}}=\SI{100}{\micro\watt}$ this condition is satisfied by
construction; departures from linearity at high modulation depth are treated
in \cref{sec:pd_discussion}.

\subsection{Band Structure of Germanium and the Direct-Gap Absorption Mechanism}
\label{subsec:pd_bandstructure}

Germanium is an indirect-gap semiconductor whose lowest conduction-band minimum
lies at the $L$-point of the Brillouin zone, separated from the valence-band
maximum at $\Gamma$ by an indirect gap
$E_{g,\mathrm{ind}}=\SI{0.66}{\electronvolt}$ at \SI{300}{\kelvin}. A secondary
conduction-band minimum at the $\Gamma$-point defines a direct gap
$E_{g,\mathrm{dir}}=\SI{0.80}{\electronvolt}$~\cite{AshcroftMermin2021,Rossler2009,Ziman1972}.
The photon energy at \SI{1310}{\nano\metre} is
$\hbar\omega=hc/\lambda\approx\SI{0.946}{\electronvolt}$, which exceeds
$E_{g,\mathrm{dir}}$ by approximately \SI{0.15}{\electronvolt}. Optical absorption
at this wavelength therefore proceeds predominantly through \emph{direct} interband
transitions at the $\Gamma$-point, for which crystal momentum is intrinsically
conserved without phonon assistance~\cite{Basu2003,Kantorovich2004}. Silicon,
whose lowest direct transition lies at \SI{3.4}{\electronvolt} and whose indirect
gap of \SI{1.12}{\electronvolt} makes it effectively transparent at O-band
wavelengths, cannot be used as the absorber. This single material-physics argument
is the entire justification for the Ge-on-Si heterostructure.

\paragraph{Heterojunction band alignment and its numerical consequences.}
The electrical consequence of the Ge/Si material interface deserves explicit
treatment because it directly conditions the boundary conditions of the
drift-diffusion problem. Owing to the lattice mismatch ($\Delta a/a\approx4.2\%$)
and the difference in band alignment, the Ge/Si heterojunction sustains a
valence-band discontinuity of $\Delta E_v\approx\SI{0.54}{\electronvolt}$ and a
conduction-band offset $\Delta E_c\approx\SI{-0.14}{\electronvolt}$~\cite{Basu2003}. For a $p^{++}$-Si anode contacting the intrinsic Ge
absorber, the large $\Delta E_v$ acts as a potential barrier suppressing hole
injection from silicon into germanium, thereby reducing the minority-carrier
diffusion contribution to dark current quantified by \cref{eq:jdark_diff}.
A thin compositionally graded transition layer of thickness
$\delta_{\mathrm{grad}}\approx\SI{5}{\nano\metre}$ at the heterojunction
removes the valence-band singularity that would otherwise prevent convergence
of the self-consistent drift-diffusion solver without perceptibly modifying
the electrostatic potential profile in the intrinsic germanium bulk.

\paragraph{Biaxial strain and the direct-gap red-shift.}
A further consequence of the Ge band structure is the sensitivity of
$E_{g,\mathrm{dir}}$ to biaxial in-plane tensile strain $\varepsilon$.
The application of biaxial tensile strain reduces the direct gap according to
the linear deformation-potential relation:
\begin{equation}
    E_{g,\mathrm{dir}}(\varepsilon)
    \approx E_{g,\mathrm{dir}}(0)
      - \bigl(2a_v + a_c\bigr)\bigl(1 - c_{12}/c_{11}\bigr)\varepsilon,
    \label{eq:bandgap_strain}
\end{equation}
where $a_v$ and $a_c$ are the valence- and conduction-band hydrostatic
deformation potentials and $c_{11}$, $c_{12}$ are the elastic stiffness
constants. The biaxial tensile strain of approximately
$0.2$--$0.3\%$ that develops in Ge epitaxial layers during post-growth
cooldown---owing to the thermal expansion coefficient mismatch between Ge and
Si---reduces $E_{g,\mathrm{dir}}$ by approximately \SI{25}{\milli\electronvolt}
relative to the unstrained bulk value, red-shifting the onset of direct-gap
absorption by roughly \SI{50}{\nano\metre} and enhancing $\kappa(\lambda)$ at
C-band wavelengths. For the O-band operating point of the present device this
correction is small but it is the principal mechanism exploited in strained-Ge
extended-wavelength detector designs.

Integrating \cref{eq:abs_density} through an absorber of length $L$ gives:
\begin{equation}
    \mathcal{A}(L) = 1 - \exp\!\bigl[-\alpha(\lambda)\,L\bigr].
    \label{eq:abs_fraction}
\end{equation}

\begin{table}[H]
\centering
\caption{Material properties of candidate photodetector absorbers at
\SI{300}{\kelvin}. The comparison isolates the material argument behind the
choice of Ge for O-band silicon photonics. $\alpha$ values are referenced to
\SI{1310}{\nano\metre}; ``N/A-CMOS'' denotes incompatibility with a standard
silicon-photonics process flow.}
\label{tab:material_compare}
\footnotesize
\begin{adjustbox}{width=\textwidth}
\begin{tabular}{lcccccc}
\toprule
Material & $E_g$ [eV] & $\alpha_{1310}$ [\si{\per\centi\metre}] &
$v_{\mathrm{sat},h}$ [\si{\centi\metre\per\second}] &
$\mu_p$ [\si{\centi\metre\squared\per\volt\per\second}] &
$\Delta a/a_{\mathrm{Si}}$ [\%] & CMOS compat. \\
\midrule
Ge & 0.66 (ind.), 0.80 (dir.) & $\sim2100$ & \SI{4e6}{} & 1900 & 4.2 & \checkmark \\
Si & 1.12 (ind.) & $\sim10$ & \SI{2.5e6}{} & 480 & 0 & N/A (transparent) \\
InGaAs (lattice-matched to InP) & 0.75 & $\sim1\times10^4$ & \SI{5e6}{} & 4500 & $>5$ & N/A-CMOS \\
Ge$_{0.92}$Sn$_{0.08}$ & $\sim0.55$ (dir.) & $>5000$ & $\sim$\SI{4e6}{} & $\sim$1500 & 4.8 & Research \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.88\linewidth]{
        Two-panel concept figure. Left panel: absorption coefficient
        $\alpha(\lambda)$ versus wavelength for Ge, Si, InGaAs, and
        GeSn, with markers at \SI{1310}{\nano\metre} and
        \SI{1550}{\nano\metre}. Right panel: schematic comparison of the Ge
        and Si band structures showing the O-band photon energy relative to
        the Ge direct gap and the much larger Si direct-transition energy.}
    \caption{Material selection argument for O-band detection on a silicon
    photonics platform. Germanium combines appreciable absorption at
    \SI{1310}{\nano\metre}, near-direct-gap behaviour, high hole saturation
    velocity, and foundry-compatible heterogeneous integration, which is why
    it remains the practical absorber of choice in \cref{subsec:pd_bandstructure}.}
    \label{fig:ge_vs_si_material}
\end{figure}

A compact material comparison is therefore sufficient to close the absorber
selection argument: Ge is not chosen because it is universally superior to all
alternatives, but because it is the only material in \cref{tab:material_compare}
that simultaneously satisfies the optical, transport, and integration
constraints of the present O-band receiver.

\subsection{Carrier Generation and Recombination}
\label{subsec:pd_generation}

The volumetric optical generation rate is:
\begin{equation}
    G_{\mathrm{opt}}(\mathbf{r})
    = \frac{\mathcal{P}_{\mathrm{abs}}(\mathbf{r})}{\hbar\omega}
    = \frac{\alpha(\lambda)\,I(\mathbf{r})}{\hbar\omega}
    \quad [\si{\per\centi\metre\cubed\per\second}].
    \label{eq:gen_rate}
\end{equation}
Three recombination mechanisms are quantitatively relevant~\cite{SzeNg2006,Basu2003}. \emph{Shockley-Read-Hall} (SRH) recombination, mediated by
deep-level trap states, is:
\begin{equation}
    U_{\mathrm{SRH}}
    = \frac{np - n_i^2}{\tau_p\!\left(n + n_1\right)
      + \tau_n\!\left(p + p_1\right)},
    \label{eq:srh}
\end{equation}
where $n_1=n_i\exp[(E_t-E_i)/kT]$ and $p_1=n_i\exp[(E_i-E_t)/kT]$.
Within the fully depleted intrinsic region at the midgap trap condition
$E_t=E_i$, \cref{eq:srh} reduces to:
\begin{equation}
    U_{\mathrm{SRH,dep}} \approx \frac{n_i}{\tau_n + \tau_p}
    \equiv \frac{n_i}{2\tau_0},
    \label{eq:srh_dep}
\end{equation}
where $\tau_0=(\tau_n+\tau_p)/2$. \emph{Auger} recombination follows
$U_{\mathrm{Auger}}=C_n n(np-n_i^2)+C_p p(np-n_i^2)$. \emph{Radiative}
recombination is modelled as:
\begin{equation}
    U_{\mathrm{rad}} = r_{\mathrm{opt}}\!\left(np - n_i^2\right),
    \label{eq:uradiative}
\end{equation}
where $r_{\mathrm{opt}}^{\mathrm{Ge}}\approx\SI{6.4e-14}{\centi\metre\cubed\per\second}$
at \SI{300}{\kelvin}---approximately four orders of magnitude smaller than for a
direct-gap III-V of comparable bandgap. In the fully depleted intrinsic region,
where $np\approx n_i^2$, the radiative rate vanishes identically and
\cref{eq:srh_dep} constitutes the sole generation mechanism. Simulated
generation and recombination rates confirm that SRH exceeds Auger by more than
two orders of magnitude and radiative recombination by more than five orders of
magnitude across $0\leq|V_{\mathrm{bias}}|\leq\SI{3}{\volt}$.
This justifies the single-parameter calibration strategy of
\cref{subsec:pd_charge}, in which only $\tau_0$ is adjusted to reproduce $I_d$.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.84\linewidth]{
        Three-panel concept figure. Panel (a): photon absorption in Ge
        creating an electron-hole pair. Panel (b): competing SRH, Auger, and
        radiative recombination pathways with their relative strengths in the
        depleted intrinsic region. Panel (c): qualitative reverse-bias trend
        showing SRH generation dominating the dark-current contribution in the
        operating range of the present device.}
    \caption{Carrier generation and recombination picture used throughout the
    electrical model. In the fully depleted intrinsic Ge layer, SRH generation
    remains the dominant dark-current mechanism, which justifies calibrating
    the device primarily through the lifetime parameter $\tau_0$.}
    \label{fig:generation_recombination}
\end{figure}

\subsection{Drift-Diffusion Transport: Transient and Steady-State Formulations}
\label{subsec:pd_transport}

The self-consistent carrier distribution and electrostatic field are governed
by the coupled Poisson and drift-diffusion equations~\cite{SzeNg2006,Yariv1997}.
Poisson's equation is:
\begin{equation}
    \nabla\cdot\!\left[\varepsilon(\mathbf{r})\,\nabla\phi(\mathbf{r},t)\right]
    = -q\!\left[p - n + N_D^+ - N_A^-\right],
    \label{eq:poisson}
\end{equation}
and the electron and hole current densities in the drift-diffusion approximation
are:
\begin{align}
    \mathbf{J}_n &= q\mu_n\,n\,\mathbf{E} + qD_n\,\nabla n,
        \label{eq:Jn}\\
    \mathbf{J}_p &= q\mu_p\,p\,\mathbf{E} - qD_p\,\nabla p,
        \label{eq:Jp}
\end{align}
where the Einstein relations $D_{n,p}=\mu_{n,p}kT/q$ hold for non-degenerate
statistics. The general, time-dependent carrier continuity equations are:
\begin{align}
    \frac{\partial n}{\partial t}
        &= \frac{1}{q}\nabla\cdot\mathbf{J}_n
           - U_{\mathrm{net}} + G_{\mathrm{opt}},
        \label{eq:cont_n_transient}\\
    \frac{\partial p}{\partial t}
        &= -\frac{1}{q}\nabla\cdot\mathbf{J}_p
           - U_{\mathrm{net}} + G_{\mathrm{opt}},
        \label{eq:cont_p_transient}
\end{align}
where $U_{\mathrm{net}}=U_{\mathrm{SRH}}+U_{\mathrm{Auger}}+U_{\mathrm{rad}}$.
Under \emph{steady-state} CW illumination, $\partial/\partial t\equiv0$:
\begin{align}
    \frac{1}{q}\nabla\cdot\mathbf{J}_n &= U_{\mathrm{net}} - G_{\mathrm{opt}},
        \label{eq:cont_n}\\
    \frac{1}{q}\nabla\cdot\mathbf{J}_p &= -(U_{\mathrm{net}} - G_{\mathrm{opt}}).
        \label{eq:cont_p}
\end{align}
The complete self-consistent system---\cref{eq:poisson} through
\cref{eq:cont_p}---is the foundational PDE set solved numerically in both the
commercial finite-element solver and the PINN framework introduced later in
\cref{sec:pd_pinn}.

\paragraph{Analytical photocurrent transfer function.}
In the frequency domain, neglecting diffusion and assuming a uniform electric
field at saturation value throughout $z\in[0,W_i]$, the Fourier amplitude of
the terminal current density admits a closed-form
expression:
\begin{align}
    J_t(\omega)
    &= \frac{q G_0}{\alpha W_i}\frac{hc}{q\lambda}
       \eta(1-R_{\mathrm{refl}})P_{\mathrm{opt}}(\omega)\nonumber\\
    &\quad\times\!
      \left\{
        \frac{e^{-\alpha W_i}-1}{\alpha W_i(\alpha W_i - j\omega\tau_{\mathrm{tr,h}})}
        + e^{-\alpha W_i}
          \frac{e^{j\omega\tau_{\mathrm{tr,h}}}-1}
               {j\omega\tau_{\mathrm{tr,h}}(\alpha W_i - j\omega\tau_{\mathrm{tr,h}})}
      \right.\nonumber\\
    &\quad\quad\left.
        + \frac{1 - e^{-\alpha W_i}}{\alpha W_i(j\omega\tau_{\mathrm{tr,e}} + \alpha W_i)}
        + \frac{1 - e^{j\omega\tau_{\mathrm{tr,e}}}}{j\omega\tau_{\mathrm{tr,e}}
          (j\omega\tau_{\mathrm{tr,e}} + \alpha W_i)}
      \right\},
    \label{eq:jt_analytical}
\end{align}
where $\tau_{\mathrm{tr,e}}=W_i/v_{\mathrm{d,sat},e}$ and
$\tau_{\mathrm{tr,h}}=W_i/v_{\mathrm{d,sat},h}$ are the electron and hole
transit times. In the limits $\omega\tau_{\mathrm{tr,h}}\ll1$ and
$\alpha W_i\gg1$, \cref{eq:jt_analytical} reduces to the rectangular-pulse
sinc-function roll-off yielding the bandwidth formula of \cref{eq:bw_tt}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.86\linewidth]{
        Two-part transport figure. Left: a carrier pair generated inside the
        intrinsic layer with electron and hole transit paths to the contacts.
        Right: equivalent-circuit derivation flow
        $h_z(t)\rightarrow H_{\mathrm{tt}}(\omega)\rightarrow
        H_{\mathrm{RC}}(\omega)\rightarrow H_{\mathrm{PD}}(\omega)$.}
    \caption{Microscopic-to-circuit derivation path for the small-signal transfer function. The transit-time response originates from the finite carrier flight time inside the intrinsic Ge region, while the RC response originates from the external electrical loading seen by the junction.}
    \label{fig:transfer_derivation}
\end{figure}

\paragraph{Derivation of the factorised small-signal transfer function.}
The compact response model of \cref{eq:H_pd} can be derived directly from the
microscopic transport picture. Consider an electron-hole pair generated at
depth $z\in[0,W_i]$ inside the intrinsic layer. Under the uniform-field,
velocity-saturated approximation, the electron and hole transit times are
\begin{equation}
    \tau_e(z)=\frac{z}{v_{\mathrm{d,sat},e}},
    \qquad
    \tau_h(z)=\frac{W_i-z}{v_{\mathrm{d,sat},h}}.
    \label{eq:tau_depth}
\end{equation}
By the Ramo-Shockley theorem, the terminal current induced by each carrier is
non-zero only while the carrier is moving, giving the local impulse response
\begin{equation}
    h_z(t)
    = \frac{qv_{\mathrm{d,sat},e}}{W_i}\,
      u(t)\,u\!\left[\tau_e(z)-t\right]
    + \frac{qv_{\mathrm{d,sat},h}}{W_i}\,
      u(t)\,u\!\left[\tau_h(z)-t\right],
    \label{eq:hz_local}
\end{equation}
where $u(t)$ is the Heaviside function. Fourier transformation yields
\begin{equation}
    H_z(\omega)
    = \frac{q}{W_i}\left[
        \frac{1-e^{-j\omega\tau_e(z)}}{j\omega}
      + \frac{1-e^{-j\omega\tau_h(z)}}{j\omega}
      \right].
    \label{eq:hz_freq}
\end{equation}
If the optical generation is approximately uniform across the depletion depth,
the transit response follows from averaging \cref{eq:hz_freq} over $z$:
\begin{equation}
    H_{\mathrm{tt}}(\omega)\propto
    \int_0^{W_i} H_z(\omega)\,\frac{\mathrm{d}z}{W_i}.
    \label{eq:htt_integral}
\end{equation}
The integral evaluates to a weighted sum of electron and hole sinc terms. In
the practically relevant limit where the slower hole branch dominates the
\SI{3}{dB} roll-off, the envelope reduces to the single-sinc form used in
\cref{eq:H_pd}. The electrical loading is then appended through the junction
capacitance and total series resistance. For a current source driving
$C_j$ into $R_{\mathrm{tot}}=R_s+R_L$,
\begin{equation}
    H_{\mathrm{RC}}(\omega)
    = \frac{1}{1+j\omega R_{\mathrm{tot}}C_j}
    = \frac{1}{1+j\omega/\omega_{\mathrm{RC}}},
    \label{eq:hrc_transfer}
\end{equation}
with $\omega_{\mathrm{RC}}=(R_{\mathrm{tot}}C_j)^{-1}$. The total response is
the product
\begin{equation}
    H_{\mathrm{PD}}(\omega)
    = H_{\mathrm{tt}}(\omega)\,H_{\mathrm{RC}}(\omega),
    \label{eq:hpd_factorised}
\end{equation}
which is the physical content hidden inside the compact form of
\cref{eq:H_pd}: transport determines the sinc-like envelope, while the external
electrical boundary condition determines the low-pass pole.

\paragraph{Scharfetter-Gummel discretisation and numerical stability.}
The finite-element discretisation of \cref{eq:Jn,eq:Jp} on a non-uniform mesh
faces a well-known numerical instability when the local P\'eclet number
$\mathrm{Pe}=q|\mathbf{E}|\Delta x/(2kT)$ exceeds unity. The
Scharfetter-Gummel scheme approximates the current between adjacent mesh nodes
$i$ and $i+1$ as:
\begin{equation}
    J_{n,i+1/2}
    = \frac{qD_n}{\Delta x}\!\left[
        B\!\left(\frac{q\Delta\phi}{kT}\right)n_{i+1}
      - B\!\left(-\frac{q\Delta\phi}{kT}\right)n_i
      \right],
    \label{eq:sg_scheme}
\end{equation}
where $B(u)=u/(e^u-1)$ is the Bernoulli function and $\Delta\phi=\phi_{i+1}-\phi_i$.
This scheme recovers centred differences for $|u|\ll1$ (low-field) and
upstream weighting for $|u|\gg1$ (high-field), providing second-order accuracy
and unconditional positivity across the \SI{30}{\kilo\volt\per\centi\metre}
field regime of the present device. The mesh refinement
criterion $\mathrm{Pe}<3$ everywhere in the germanium bulk is the operative
test for mesh convergence.

\subsection{Performance Metrics: Rigorous Definitions and Physical Interpretation}
\label{subsec:pd_metrics}

\subsubsection{Responsivity and Quantum Efficiency}
\label{subsubsec:pd_responsivity}

The \emph{external responsivity} $\mathcal{R}$ is:
\begin{equation}
    \mathcal{R} \equiv \frac{I_{\mathrm{ph}}}{P_{\mathrm{opt}}}
    \quad [\si{\ampere\per\watt}],
    \label{eq:responsivity}
\end{equation}
with theoretical upper bound:
\begin{equation}
    \mathcal{R}_{\max} = \frac{q\lambda}{hc}
    \approx \SI{1.057}{\ampere\per\watt}
    \quad\text{at \SI{1310}{\nano\metre}}.
    \label{eq:rmax}
\end{equation}
The external quantum efficiency (EQE) $\eta_{\mathrm{ext}}$ and internal quantum
efficiency (IQE) $\eta_{\mathrm{int}}$ are:
\begin{align}
    \eta_{\mathrm{ext}}
    &= \mathcal{R}\cdot\frac{hc}{q\lambda},
    \label{eq:eqe}\\
    \eta_{\mathrm{int}}
    &= \frac{I_{\mathrm{ph}}/q}{P_{\mathrm{abs}}/\hbar\omega}.
    \label{eq:iqe}
\end{align}
In the fully depleted intrinsic region at saturation field, the drift transit
time $\tau_{\mathrm{tr}}=W_i/v_{\mathrm{d,sat},h}$ is much shorter than the
SRH lifetime $\tau_0$, giving a carrier collection efficiency
$\xi\approx1-\tau_{\mathrm{tr}}/\tau_0\approx1$. The more general form of the
EQE chain is:
\begin{equation}
    \eta_{\mathrm{ext}}
    = (1 - R_{\mathrm{refl}})\,\xi\,\eta_c\,
      \bigl(1 - e^{-\alpha(\lambda)\,L}\bigr),
    \label{eq:qe_full}
\end{equation}
where $R_{\mathrm{refl}}$ is the Si/Ge entry-facet reflectance and $\xi$
degrades measurably from unity when the bias is reduced toward zero, making
the factored form of \cref{eq:qe_full} the natural framework for evaluating
low-bias operating points---motivated by the energy-per-bit metric $e_c\propto|V_{\mathrm{bias}}|\,\mathcal{R}$
introduced in \cref{sec:pd_discussion}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.84\linewidth]{
        Two-panel metrics figure. Left panel: photocurrent versus input
        optical power, showing the linear responsivity slope in the low-power
        regime and saturation at high power. Right panel: responsivity versus
        wavelength, highlighting the short-wavelength reduction from excess
        carrier energy and the long-wavelength roll-off near the absorption
        edge.}
    \caption{Responsivity viewed in both its power-domain and spectral forms.
    The low-power slope of the $I$-$P$ curve defines $\mathcal{R}$, whereas
    the wavelength dependence determines the useful operating window around the
    O-band.}
    \label{fig:responsivity_concept}
\end{figure}

\subsubsection{Dark Current}
\label{subsubsec:pd_dark}

Dark current $I_d$ flows through the reverse-biased junction in the absence of
optical excitation. Three generation-recombination mechanisms
contribute~\cite{SzeNg2006}. The \emph{SRH generation} current density within
the depleted intrinsic layer of thickness $W_i$ follows from \cref{eq:srh_dep}:
\begin{equation}
    J_{\mathrm{SRH}} = q\,U_{\mathrm{SRH,dep}}\,W_i
    = \frac{q\,n_i\,W_i}{2\tau_0}.
    \label{eq:jdark_srh}
\end{equation}
\emph{Minority-carrier diffusion} from the quasi-neutral contact regions contributes:
\begin{equation}
    J_{\mathrm{diff}}
    = \frac{qD_p\,n_i^2}{L_p\,N_D} + \frac{qD_n\,n_i^2}{L_n\,N_A},
    \label{eq:jdark_diff}
\end{equation}
where $L_p=\sqrt{D_p\tau_p}$ and $L_n=\sqrt{D_n\tau_n}$.
\emph{Interface recombination} at the Ge/Si heterojunction contributes:
\begin{equation}
    J_{\mathrm{surf}} \approx \frac{q\,n_i\,v_s}{2}.
    \label{eq:jdark_surf}
\end{equation}
The total dark current, including both bulk and surface (perimeter) contributions,
is:
\begin{equation}
    I_d = A_j\!\left(J_{\mathrm{SRH}} + J_{\mathrm{diff}}\right)
    + P_j\,q\,n_i\,v_s/2,
    \label{eq:idark_decomposed}
\end{equation}
where $P_j=2(W_m+L)$ is the junction mesa perimeter. This perimeter scaling
has a direct consequence for miniaturisation: as $A_j$ is reduced to decrease
$C_j$ and improve bandwidth, the area-to-perimeter ratio $A_j/P_j$ decreases,
increasing the fractional contribution of the surface term. For the present
device, $A_j/P_j\approx\SI{0.43}{\micro\metre}$; a future device scaled to
$A_j=\SI{2}{\micro\metre\squared}$ would reduce this ratio toward
\SI{0.17}{\micro\metre}, at which point surface passivation through
Ge$_x$O$_y$ or Al$_2$O$_3$ becomes a primary design lever.
Additionally, the minimum achievable dark current density in Ge-on-Si epitaxial
material is set by the threading dislocation density (TDD): TDD
$\approx\SI{5e7}{\per\centi\metre\squared}$ corresponds to a dark current
density floor of order \SI{10}{\milli\ampere\per\centi\metre\squared} at
\SI{-1}{\volt}, and the calibrated $\tau_0=\SI{5}{\nano\second}$ in
\cref{subsec:pd_charge} implicitly reflects this value.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Two-panel dark-current figure. Left panel: decomposition of total dark
        current into SRH, diffusion, and surface terms versus reverse bias.
        Right panel: area scaling versus perimeter scaling for large and small
        junctions, showing why surface effects grow in importance as the mesa
        footprint shrinks.}
    \caption{Dark-current mechanisms of the reverse-biased Ge-on-Si PIN.
    The present device remains bulk-SRH dominated at the chosen footprint, but
    perimeter-driven surface generation becomes progressively more important as
    the junction is aggressively miniaturised.}
    \label{fig:dark_current_concept}
\end{figure}

\subsubsection{Electrical Bandwidth}
\label{subsubsec:pd_bw}

The frequency response is limited by two physically distinct mechanisms.
In the absence of deliberate inductive peaking, the combined baseline
bandwidth is~\cite{SalehTeich1991}:
\begin{equation}
    f_{3\,\mathrm{dB}}
    = \left(\frac{1}{f_{\mathrm{tt}}^2}
      + \frac{1}{f_{\mathrm{RC}}^2}\right)^{-1/2}.
    \label{eq:bw_total}
\end{equation}

\paragraph{Transit-time limit.}
The most transparent closed-form estimate is the one-carrier lower bound
obtained by assuming that the slowest carrier branch controls the roll-off.
With hole saturation velocity $v_{\mathrm{d,sat},h}\approx\SI{4e6}{\centi\metre\per\second}$
and electron saturation velocity $v_{\mathrm{d,sat},e}\approx\SI{6e6}{\centi\metre\per\second}$,
the corresponding transit-time-limited 3\,dB bandwidth from Fourier analysis
of a rectangular current impulse response is:
\begin{equation}
    f_{\mathrm{tt}} = \frac{0.45\,v_{\mathrm{d,sat},h}}{W_i},
    \label{eq:bw_tt}
\end{equation}
where the coefficient $0.45$ arises from the sinc-like spectral roll-off and
reflects the asymmetric electron-hole velocity ratio
$v_{\mathrm{d,sat},e}/v_{\mathrm{d,sat},h}\approx1.5$.
For $W_i=\SI{350}{\nano\metre}$:
$f_{\mathrm{tt}}\approx\SI{51}{\giga\hertz}$.

This value is intentionally conservative. It is a geometry-screening lower
bound, not the absolute transport ceiling of the realised vertical Ge PIN.
The full two-carrier response of \cref{eq:jt_analytical} is broader than the
single-carrier lower bound because electrons and holes induce terminal current
simultaneously, and the experimentally relevant RF response further benefits
from the on-electrode inductance $L_p$ of the packaged structure. For the
\SI{350}{\nano\metre} intrinsic region used as the reference stack in later
sections, the benchmark-calibrated transport ceiling
is in the \SIrange{110}{120}{\giga\hertz} class, with a measured
$f_{3\,\mathrm{dB}}=\SI{103}{\giga\hertz}$ at \SI{-1}{\volt} once the
U-shaped electrode and matched inductive peaking are both present. The
lower-bound form of \cref{eq:bw_tt} is nevertheless retained throughout this
chapter for rapid geometry sweeps because it makes the penalty of
$W_i\uparrow$ explicit before RF peaking is invoked.

\paragraph{RC limit.}
The junction capacitance of the fully depleted intrinsic region is:
\begin{equation}
    C_j = \frac{\varepsilon_0\varepsilon_{\mathrm{Ge}}\,A_j}{W_i},
    \label{eq:cj}
\end{equation}
where $\varepsilon_{\mathrm{Ge}}\approx16.2$ and $A_j=W_m\times L$.
The RC-limited bandwidth is:
\begin{equation}
    f_{\mathrm{RC}} = \frac{1}{2\pi R_{\mathrm{tot}}\,C_j},
    \label{eq:bw_rc}
\end{equation}
where $R_{\mathrm{tot}}=R_s+R_L$ and $R_L=\SI{50}{\ohm}$.

\paragraph{Optimal intrinsic-layer thickness.}
Setting $f_{\mathrm{tt}}(W_i^*)=f_{\mathrm{RC}}(W_i^*)$ and substituting
\cref{eq:bw_tt,eq:bw_rc,eq:cj}:
\begin{equation}
    \frac{0.45\,v_{\mathrm{d,sat},h}}{W_i^*}
    = \frac{W_i^*}{2\pi R_{\mathrm{tot}}\,\varepsilon_0\varepsilon_{\mathrm{Ge}}\,A_j},
    \label{eq:wi_optimal_eq}
\end{equation}
which yields:
\begin{equation}
    W_i^* = \sqrt{0.9\pi\,R_{\mathrm{tot}}\,\varepsilon_0\varepsilon_{\mathrm{Ge}}
             \,v_{\mathrm{d,sat},h}\,A_j}.
    \label{eq:wi_optimal}
\end{equation}
Substituting $R_{\mathrm{tot}}=\SI{60}{\ohm}$, $\varepsilon_{\mathrm{Ge}}=16.2$,
$v_{\mathrm{d,sat},h}=\SI{4e6}{\centi\metre\per\second}$, and
$A_j=\SI{6}{\micro\metre\squared}$ gives
$W_i^*\approx\SI{310}{\nano\metre}$, within \SI{11}{\percent} of the chosen
$W_i=\SI{350}{\nano\metre}$---confirming the device is positioned near the
bandwidth optimum consistent with the $R_{\mathrm{tot}}=\SI{50}{\ohm}$ standard
load. The expression \cref{eq:wi_optimal} also reveals a
fundamental scaling law: since $W_i^*\propto\sqrt{A_j}$, shrinking the junction
area by a factor of four allows $W_i^*$ to be halved, raising the balanced
bandwidth at the new optimum by approximately the same factor. The small-signal
photocurrent transfer function, representing the photodetector as an LTI channel,
is well-approximated by the factorised form:
\begin{equation}
    H_{\mathrm{PD}}(f)
    = \mathrm{sinc}\!\left(\frac{f}{f_{\mathrm{tt}}}\right)
    \cdot\frac{1}{1+i\,f/f_{\mathrm{RC}}},
    \label{eq:H_pd}
\end{equation}
where $\mathrm{sinc}(x)=\sin(\pi x)/(\pi x)$. The first zero of
$|H_{\mathrm{tt}}(f)|$ occurs at $f=f_{\mathrm{tt}}/0.45\approx\SI{113}{\giga\hertz}$
for the present device. The ISI-free reception condition without equalisation
is $f_{\mathrm{Nyq}}=R_s/2\leq\beta\,f_{3\,\mathrm{dB}}$ where
$\beta\approx0.7$. Violation requires a feed-forward equaliser (FFE) whose
tap count scales approximately as $N_{\mathrm{tap}}\sim2f_{\mathrm{Nyq}}/f_{3\,\mathrm{dB}}$,
so that every doubling of $f_{3\,\mathrm{dB}}$ relative to $f_{\mathrm{Nyq}}$
halves the DSP complexity~\cite{ProakisSalehi2007}.

Equation~\cref{eq:H_pd} is therefore a deliberately simplified, non-peaked
baseline. For the benchmark-calibrated U-shaped device, the electrical
sub-network is more accurately represented by a series branch
$Z_s(\omega)=R_s+j\omega L_p$ driving a terminated capacitive load
$Z_L(\omega)=\left(R_L^{-1}+j\omega C_p\right)^{-1}$ while the reverse-biased
junction impedance is $Z_j(\omega)\approx(j\omega C_j)^{-1}$. Current division
then gives the peaked electrical transimpedance
\begin{equation}
    H_{\mathrm{elec,pk}}(\omega)
    = \frac{V_{\mathrm{out}}(\omega)}{I_{\mathrm{ph}}(\omega)}
    = \frac{Z_j(\omega)\,Z_L(\omega)}
           {Z_j(\omega)+Z_s(\omega)+Z_L(\omega)},
    \label{eq:helec_pk}
\end{equation}
and the full benchmark-calibrated detector response becomes
\begin{equation}
    H_{\mathrm{PD,pk}}(\omega)
    = H_{\mathrm{tt}}(\omega)\,H_{\mathrm{elec,pk}}(\omega).
    \label{eq:H_pd_pk}
\end{equation}
In the limits $L_p\rightarrow0$ and $C_p\rightarrow0$,
\cref{eq:H_pd_pk} reduces continuously to the first-order form of
\cref{eq:H_pd}. The distinction between \cref{eq:H_pd} and
\cref{eq:H_pd_pk} is central to this chapter: the former is used for fast
trade-off screening, whereas the latter is used when projecting the validated
single-U device toward the \SI{100}{\giga\hertz}-class operating regime.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Two-panel frequency-response plot. Left panel: normalised photocurrent
        transfer function $|H_{\mathrm{PD}}(f)|^2$ in dB from 1 to
        \SI{300}{\giga\hertz} showing three curves: $|H_{\mathrm{tt}}(f)|^2$
        (dash-dot sinc$^2$), $|H_{\mathrm{RC}}(f)|^2$ (dashed single-pole),
        and the product $|H_{\mathrm{PD}}(f)|^2$ (solid). Vertical dashed lines
        mark $f_{\mathrm{tt}}$, $f_{\mathrm{RC}}$, $f_{3\,\mathrm{dB}}$, and
        $f_{\mathrm{Nyq}}=\SI{50}{\giga\hertz}$ at $R_s=\SI{100}{\giga\baud}$.
        Right panel: linear-scale frequency axis from 0 to \SI{200}{\giga\hertz}
        with shaded ISI-penalty region between $f_{\mathrm{Nyq}}$ and
        $f_{3\,\mathrm{dB}}$.}
    \caption{LTI channel representation of the photodetector from
    \cref{eq:H_pd}. The transit-time sinc-roll-off and RC pole are shown as
    independent contributions; their product defines the effective bandwidth
    $f_{3\,\mathrm{dB}}$ and the ISI margin available to the receiver DSP.
    The $\SI{100}{\giga\baud}$ Nyquist frequency of \SI{50}{\giga\hertz}
    falls well within the passive passband, confirming ISI-free operation.}
    \label{fig:H_pd_channel}
\end{figure}

\subsubsection{Capacitance and Series Resistance}
\label{subsubsec:pd_cres}

The series resistance $R_s$ in the vertical n-i-p architecture is dominated by
the lateral conduction path through the $p^{++}$-Si platform:
\begin{equation}
    R_{s,\mathrm{anode}} \approx \frac{\rho_{\mathrm{Si}}}{t_{\mathrm{Si}}}
    \cdot\frac{\ell_a}{w_a},
    \label{eq:rs_anode}
\end{equation}
where $t_{\mathrm{Si}}=\SI{220}{\nano\metre}$ is the platform thickness.
The RC time constant is:
\begin{equation}
    \tau_{\mathrm{RC}} = R_s\,C_j,
    \label{eq:tau_rc}
\end{equation}
from which $f_{\mathrm{RC}}=(2\pi\tau_{\mathrm{RC}})^{-1}$.

The small-signal admittance $Y(\omega)=I_{\mathrm{AC}}/V_{\mathrm{AC}}$
exhibits two distinct plateau levels in $\mathrm{Im}[Y(\omega)]/(2\pi f)$:
a low-frequency plateau at $C_1+C_2$ and a high-frequency plateau at $C_2$
alone, where $C_1$ is the intrinsic junction capacitance and $C_2$ is the
parasitic contribution from the first interconnect metal layer. For the
vertical architecture with $A_j=\SI{1}{\micro\metre}\times\SI{6}{\micro\metre}$,
the parallel-plate estimate
$C_1\approx\varepsilon_0\varepsilon_{\mathrm{Ge}}A_j/W_i\approx\SI{1.8}{\femto\farad}$
is verified by the low-to-high-frequency plateau difference extracted
numerically. Removing the first copper metal layer reduces
$C_2$ by more than \SI{35}{\percent}, confirming that electrode layout
optimisation must simultaneously minimise the lateral conduction path $\ell_a$
and the overlap area between the first metal layer and the doped silicon platform.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Two-panel RC figure. Left panel: simplified equivalent circuit showing
        $C_j$, $R_s$, parasitic capacitance, and the \SI{50}{\ohm} load.
        Right panel: qualitative capacitance and RC-limited bandwidth trends
        versus junction area, illustrating the penalty paid when optical
        absorption is pursued through a larger footprint.}
    \caption{Capacitance-resistance interpretation of the vertical PIN
    photodetector. The key RC penalty comes from simultaneously increasing the
    depleted junction area and the lateral access resistance, which is why
    layout and footprint must be optimised together.}
    \label{fig:rc_concept}
\end{figure}

\subsubsection{Noise Spectral Densities and Noise-Equivalent Power}
\label{subsubsec:pd_noise}

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.86\linewidth]{
        Four-panel noise concept figure. Discrete carrier arrivals, Poisson
        counting statistics, time-domain current fluctuations, and flat shot
        versus thermal noise spectral densities are shown on a common causal
        path from charge discreteness to electrical noise PSD.}
    \caption{Figure-first explanation of noise origin. Shot noise is not an abstract fitting parameter but the continuum limit of the counting fluctuations of discrete carriers arriving at the terminal.}
    \label{fig:shot_noise_origin}
\end{figure}

\paragraph{Origin of shot noise from carrier counting statistics.}
The physical origin of \cref{eq:shot} is the randomness of discrete carrier
arrivals. Let $N_T$ denote the number of carriers collected during an
observation interval $T$. If the arrivals are statistically independent, then
$N_T$ is Poisson distributed with mean
$\mathbb{E}[N_T]=IT/q$ and variance
$\mathrm{Var}[N_T]=IT/q$. The collected charge is $Q_T=qN_T$, so
\begin{equation}
    \mathrm{Var}[Q_T] = qIT.
    \label{eq:var_charge_poisson}
\end{equation}
Dividing by $T^2$ converts charge variance to variance of the time-averaged
current over the interval,
\begin{equation}
    \mathrm{Var}\!\left[\bar{I}_T\right]
    = \frac{qI}{T}.
    \label{eq:var_current_poisson}
\end{equation}
Passing from the finite-time variance to the one-sided continuous spectral
density yields the standard result
$S_{\mathrm{shot}}=2qI$. The factor of two is the conversion from the
two-sided to one-sided PSD convention used throughout this chapter.

\emph{Shot noise} arises from the quantum-mechanical discreteness of charge
carriers:
\begin{equation}
    S_{\mathrm{shot}}(f) = 2q\!\left(I_d + I_{\mathrm{ph}}\right)
    \quad [\si{\ampere\squared\per\hertz}].
    \label{eq:shot}
\end{equation}
\emph{Johnson (thermal) noise} from the load resistance contributes:
\begin{equation}
    S_{\mathrm{thermal}}(f) = \frac{4kT}{R_L}
    \quad [\si{\ampere\squared\per\hertz}].
    \label{eq:thermal}
\end{equation}
In practice the photodetector is followed by a transimpedance amplifier (TIA),
and the combined noise of the assembly is characterised through the one-sided
input-referred noise current PSD:
\begin{equation}
    i_n^2 = S_{\mathrm{shot}} + S_{\mathrm{thermal}} + S_{\mathrm{TIA}}
    \quad [\si{\ampere\squared\per\hertz}],
    \label{eq:in_total}
\end{equation}
where $S_{\mathrm{TIA}}=4kT/R_F+(2\pi f)^2C_{\mathrm{in}}^2S_{v,\mathrm{amp}}$
for a resistive-feedback transimpedance stage with feedback resistance $R_F$
and total input capacitance $C_{\mathrm{in}}=C_j+C_{\mathrm{par}}+C_{\mathrm{gate}}$.
Integrating over the detection bandwidth gives the total noise variance:
\begin{equation}
    \sigma_n^2 = i_n^2\,B_e
    \quad [\si{\ampere\squared}],
    \label{eq:sigma_n}
\end{equation}
a compact expression that consolidates all noise contributions into a single
measurable scalar. For the simulated
$I_d=\SI{1.3}{\nano\ampere}$, the dark-current shot noise evaluates to
$i_n\approx\SI{0.645}{\pico\ampere\per\sqrt\hertz}$, while practical TIA
stages in \SI{90}{\nano\metre} CMOS achieve
$\SI{10}{}$--$\SI{20}{\pico\ampere\per\sqrt\hertz}$ over bandwidths exceeding
\SI{100}{\giga\hertz}, so $S_{\mathrm{TIA}}$ dominates $S_{\mathrm{shot}}$ by
approximately two orders of magnitude---implying that further reduction of $I_d$
below \SI{1.3}{\nano\ampere} yields negligible improvement in $\sigma_n^2$.
The \emph{noise-equivalent power} (NEP) and \emph{specific detectivity} $D^*$
are:
\begin{align}
    \mathrm{NEP}
    &= \frac{\sqrt{S_{\mathrm{shot}} + S_{\mathrm{thermal}}}}{\mathcal{R}}
    \quad [\si{\watt\per\sqrt\hertz}],
    \label{eq:nep}\\
    D^*_{\mathrm{shot}}
    &= \frac{\mathcal{R}\sqrt{A_j}}{\sqrt{2qI_d}}.
    \label{eq:dstar}
\end{align}

\paragraph{Other noise and loss terms, and why some are neglected at the device scale.}
Not every fluctuation mechanism enters the measured receiver noise equally.
Generation-recombination noise from a trap ensemble with characteristic
lifetime $\tau_{\mathrm{gr}}$ has a Lorentzian form
\begin{equation}
    S_{\mathrm{gr}}(f)
    \propto \frac{1}{1+(2\pi f\tau_{\mathrm{gr}})^2},
    \label{eq:gr_noise}
\end{equation}
while low-frequency interface noise follows the approximate law
$S_{1/f}(f)\propto I^2/f^{\gamma}$ with $\gamma\approx1$. Both therefore fall
below relevance in the tens-of-GHz band of interest here. Optical-source noise
must be separated conceptually from device noise: laser relative-intensity noise
(RIN) enters as
\begin{equation}
    S_{\mathrm{RIN}}(f) = \mathcal{R}^2P_{\mathrm{opt}}^2\,\mathrm{RIN},
    \label{eq:rin_noise}
\end{equation}
and can become important at the link level even when the detector itself is
quiet. By contrast, taper insertion loss, waveguide propagation loss,
free-carrier absorption in the doped contact layer, Fresnel reflection, and
incomplete carrier collection do not create a new electrical noise PSD by
themselves; they reduce the signal term $I_{\mathrm{ph}}=\mathcal{R}P_{\mathrm{opt}}$,
and thereby degrade SNR indirectly. This distinction is important later in
\cref{sec:pd_system}, where photodetector noise and optical-link penalties must
be budgeted separately.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Two-panel noise contribution plot. Left panel: noise current PSD
        $i_n^2(f)$ [\si{\ampere\squared\per\hertz}] in dB versus frequency
        from 0 to \SI{200}{\giga\hertz}, showing three stacked contributions:
        $S_{\mathrm{shot}}=2qI_d$ (flat blue), $S_{\mathrm{thermal}}=4kT/R_L$
        (flat green), and representative $S_{\mathrm{TIA}}$ with $f^2$ roll-up
        (red). Right panel: total noise variance $\sigma_n^2=i_n^2 B_e$ as a
        function of $B_e$ from 1 to \SI{200}{\giga\hertz}, with three curves:
        PD-only noise (shot+thermal), full assembly noise (adding TIA), and
        the shot-noise-only floor. Annotated at $B_e=f_{3\,\mathrm{dB}}=\SI{103}{\giga\hertz}$.}
    \caption{Noise decomposition of the photodetector--TIA assembly from
    \cref{eq:in_total,eq:sigma_n}. The dark-current shot noise of
    $I_d=\SI{1.3}{\nano\ampere}$ contributes negligibly to the total
    $\sigma_n^2$ at \SI{103}{\giga\hertz}, establishing TIA noise as the
    dominant design lever for sensitivity optimisation.}
    \label{fig:noise_decomposition}
\end{figure}

\section{Literature Review and State-of-the-Art Positioning}
\label{sec:pd_literature}

\subsection{Device Evolution: From Photoconductor to Waveguide-Integrated PIN}
\label{subsec:pd_evolution_bridge}

Before reviewing the experimental record, the device choice itself must be
justified. The waveguide-integrated Ge-on-Si PIN is not merely a historical
endpoint; it is the architecture that remains after simpler and apparently more
aggressive alternatives are eliminated by the metric framework of
\cref{sec:pd_concept}. The logic is progressive: each stage fixes a specific
deficiency of the previous one, and each transition can be traced directly to
the governing equations already established in Section~\ref{sec:pd_concept}.

\paragraph{Stage I --- The photoconductor: gain at the cost of speed.}
The photoconductor is the simplest optical transducer because it requires no
junction and no depletion engineering. Its apparent advantage is internal gain:
\begin{equation}
    G_{\mathrm{PC}} = \frac{\tau_r}{\tau_{\mathrm{tr}}},
    \qquad
    \tau_{\mathrm{tr}} = \frac{L^2}{\mu_n V_b},
    \label{eq:pc_gain}
\end{equation}
where $\tau_r$ is the recombination lifetime and $\tau_{\mathrm{tr}}$ is the
carrier transit time between the ohmic contacts. The problem is that the same
lifetime that enables gain also fixes the bandwidth:
\begin{equation}
    f_{3\,\mathrm{dB,PC}} = \frac{1}{2\pi\tau_r}.
    \label{eq:pc_bw}
\end{equation}
For germanium, a lifetime in the microsecond class implies a bandwidth in the
kilohertz-to-megahertz regime, which is many orders of magnitude below the
\SI{100}{\giga\baud}-class target considered here. The photoconductor is
therefore rejected by the gain-bandwidth product before any detailed geometry
optimisation is attempted.

\paragraph{Stage II --- The PN photodiode: introducing the depletion field.}
The PN junction fixes the most serious weakness of the photoconductor by
creating an internal electric field that sweeps carriers rather than waiting for
them to diffuse across a biased resistor. Yet the PN device still retains a
slow component because photocarriers generated in the quasi-neutral regions must
diffuse back to the depletion edge. The resulting response can be written as
\begin{equation}
    H_{\mathrm{PN}}(f) =
    H_{\mathrm{drift}}(f)\,
    \frac{1}{1+j2\pi f\tau_D},
    \label{eq:hpn}
\end{equation}
where $\tau_D$ is the diffusion-tail time constant. At moderate symbol rates
this penalty is manageable; at the \SI{50}{}--\SI{100}{\giga\hertz} electrical
bandwidth class it is not. The PN device therefore solves the photoconductor's
speed mechanism only partially.

\paragraph{Stage III --- The PIN photodiode: eliminating the diffusion tail.}
The PIN structure removes that residual limitation by inserting an intrinsic
layer and forcing the useful optical generation to occur inside a fully
depleted region. Once the full intrinsic thickness is depleted, the electric
field is approximately uniform:
\begin{equation}
    E(x) \approx \frac{|V_{\mathrm{bias}}|+V_{\mathrm{bi}}}{W_i},
    \qquad x\in[0,W_i].
    \label{eq:pin_efield_uniform}
\end{equation}
The diffusion tail of \cref{eq:hpn} is then suppressed and the bandwidth is
reduced to the competition already formalised through
\cref{eq:bw_tt,eq:bw_rc}: transit time improves when $W_i$ shrinks, whereas RC
penalty worsens when the junction footprint grows. That is the key structural
reason the PIN, rather than the photoconductor or the simple PN diode, remains
the default high-speed receiver device.

\paragraph{Why PIN rather than APD?}
An avalanche photodiode appears attractive because it multiplies the
responsivity:
\begin{equation}
    \mathcal{R}_{\mathrm{APD}} = M\,\mathcal{R}_{\mathrm{PIN}},
    \label{eq:r_apd}
\end{equation}
but that gain is not free. The avalanche build-up time lowers the usable
bandwidth as $M$ increases, while the excess noise factor in Ge rises rapidly
because the hole and electron ionisation coefficients are too similar for a
low-noise multiplication process. At the present operating point, the receiver
needs the bandwidth headroom more urgently than it needs internal gain, which
is why the low-bias PIN remains preferable to the higher-voltage APD.

\paragraph{Stage IV --- Why germanium on silicon?}
The choice of absorber is constrained by the O-band photon energy. The material
requirement can be written compactly as
$E_{g,\mathrm{dir}}^{\mathrm{Ge}} < hc/\lambda_{1310}
\ll E_{g,\mathrm{dir}}^{\mathrm{Si}}$, which is simply another way of saying
that germanium absorbs strongly where silicon is effectively transparent.
Through \cref{eq:abs_fraction}, this gives a practical absorber length in the
few-micrometre range instead of the prohibitively long interaction length that
silicon would require. Section~\ref{subsec:pd_bandstructure} and
\cref{tab:material_compare} then complete the remaining argument: Ge preserves
CMOS compatibility while still offering the absorption coefficient and carrier
transport needed for a compact O-band device.

\paragraph{Stage V --- Why waveguide coupling rather than surface illumination?}
Once the PIN and the Ge absorber are fixed, the final architectural decision is
the illumination geometry. In a surface-normal detector, the thickness that
sets absorption is the same thickness that sets transit time. The constraints
therefore conflict directly: the Beer-Lambert requirement
$1-\exp(-\alpha W_i)\gtrsim0.9$ pushes $W_i$ toward the multi-micrometre range,
whereas the transit-time requirement from \cref{eq:bw_tt} pushes $W_i$ toward
the sub-micrometre range. At \SI{1310}{\nano\metre}, this incompatibility is
severe enough that the same dimension cannot satisfy both constraints.
Waveguide coupling resolves the conflict by making the absorption dimension
$L$ longitudinal and the transport dimension $W_i$ vertical:
\begin{equation}
    \mathcal{A}(L) = 1-\exp(-\alpha L),
    \qquad
    f_{\mathrm{tt}} \propto \frac{1}{W_i}.
    \label{eq:wg_orthogonality}
\end{equation}
That orthogonality is the decisive refinement: absorber length and depletion
thickness become independently tunable, which is precisely what enables the
present vertical PIN architecture.

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.9\linewidth}
\textbf{Figure Placeholder --- Device Evolution from Photoconductor to Waveguide-Integrated PIN}\\[4pt]
\textbf{Prompt:}
\textit{Five-stage narrative figure laid out left-to-right or top-to-bottom.
Stage 1: photoconductor with two ohmic contacts, surface illumination, long
carrier lifetime, and a gain-bandwidth annotation using
$G_{\mathrm{PC}}=\tau_r/\tau_{\mathrm{tr}}$ and
$f_{3\,\mathrm{dB,PC}}=1/(2\pi\tau_r)$. Stage 2: PN photodiode cross-section
showing depletion region, built-in field, and a slow diffusion tail in the
impulse response. Stage 3: PIN photodiode cross-section with fully depleted
intrinsic region, uniform field across $W_i$, and a clean drift-limited pulse.
Add a side note comparing PIN and APD, making clear that APD improves
responsivity through multiplication $M$ but loses bandwidth and adds excess
noise. Stage 4: material-choice panel contrasting Ge and Si at
\SI{1310}{\nano\metre}, with the O-band photon energy above the Ge direct-gap
threshold and below the Si direct-transition threshold. Stage 5: geometry panel
contrasting surface-normal illumination against waveguide coupling, explicitly
showing that waveguide coupling decouples absorber length $L$ from depletion
thickness $W_i$. Use arrows and short equations so that the reader can follow
the causal chain without reading the surrounding text first.}
\end{minipage}}
\caption{Progressive elimination of alternative detector architectures. Each
stage fixes a specific physical limitation of the previous one, leaving the
waveguide-integrated Ge-on-Si PIN as the architecture compatible with the
O-band, low-voltage, high-bandwidth target of this chapter.}
\label{fig:pd_evolution_ladder}
\end{figure}

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.9\linewidth}
\textbf{Figure Placeholder --- Surface-Normal Illumination versus Waveguide Coupling}\\[4pt]
\textbf{Prompt:}
\textit{Four-panel figure. Panel (a): surface-normal PIN cross-section with
light propagation and carrier collection both vertical, annotated to show the
conflict between absorption thickness and transit-time thickness. Panel (b):
waveguide-coupled PIN side view showing optical propagation along the waveguide
axis and carrier collection along the growth axis, with $L$ and $W_i$ marked as
independent dimensions. Panel (c): mode-transfer sketch comparing abrupt
butt-coupling to taper-assisted evanescent coupling, including qualitative
reflection and insertion-loss annotations. Panel (d): design-space plot or
schematic map highlighting the feasible region enabled by waveguide coupling,
with absorption improving with $L$ and bandwidth improving as $W_i$ shrinks.
The key message must be that waveguide coupling restores independent control of
the optical and electrical dimensions.}
\end{minipage}}
\caption{Waveguide coupling is the decisive architectural refinement because it
decouples the absorption dimension from the transit-time dimension. That single
geometric change turns an overconstrained surface-normal problem into a
two-parameter design space.}
\label{fig:wg_vs_sni}
\end{figure}

\subsection{Review of Waveguide-Integrated Ge-on-Si Photodetectors}
\label{subsec:pd_history}

The Ge-on-Si waveguide photodetector field has evolved over approximately two
decades through successive generations of electrode and junction engineering,
each interpretable through the metric framework of \cref{sec:pd_concept}.
What follows is a causal narrative: each development is evaluated in terms of
which specific term in \cref{eq:bw_total,eq:idark_decomposed,eq:qe_full} it
addressed and why a residual performance gap persisted.

The landmark demonstration of Vivien~\textit{et al.}~\cite{Vivien2009}
established a silicon-on-insulator (SOI) waveguide-coupled Ge p-i-n detector
with $\mathcal{R}\approx\SI{1.0}{\ampere\per\watt}$ and
$f_{3\,\mathrm{dB}}=\SI{42}{\giga\hertz}$ at the O-band, confirming that
direct-gap absorption at the $\Gamma$-point translates into competitive
responsivity on the silicon platform. The \SI{42}{\giga\hertz} result was
RC-limited: the electrode geometry left $R_s$ at a value that caused
$f_{\mathrm{RC}}$ to dominate over $f_{\mathrm{tt}}$.
Ang~\textit{et al.}~\cite{Ang2010} subsequently demonstrated evanescently
coupled Ge-on-SOI fabricated at low thermal budget to remain CMOS-compatible,
establishing process integration as an additional hard constraint.

The mid-2010s were characterised by systematic reductions of $R_s$ through
contact geometry engineering. Chen~\textit{et al.}~\cite{Chen2015} demonstrated
a silicon-contacted Ge p-i-n delivering $\mathcal{R}=\SI{0.74}{\ampere\per\watt}$
and $f_{3\,\mathrm{dB}}=\SI{35}{\giga\hertz}$. The same group subsequently achieved
$f_{3\,\mathrm{dB}}=\SI{67}{\giga\hertz}$ at only \SI{1}{\volt} reverse bias
through electrode repositioning~\cite{Chen2016Si}, reducing $\ell_a$ in
\cref{eq:rs_anode} until $f_{\mathrm{RC}}$ no longer dominated \cref{eq:bw_total}.
Novack~\textit{et al.}~\cite{Novack2013} achieved $f_{3\,\mathrm{dB}}=\SI{60}{\giga\hertz}$
by introducing wire-bond inductance as an equivalent-circuit peaking element.
It is instructive to situate this strategy within a broader equivalent-circuit
taxonomy: three canonical peaking topologies---series gain peaking, enhanced series
gain peaking, and shunt peaking---produce qualitatively different modifications to
the small-signal transfer function~\cite{Yan2024}. In series gain peaking,
an inductance inserted in series with $R_L$ forms an LC resonance with
$C_j+C_{\mathrm{par}}$ at $\omega_0=1/\sqrt{L(C_j+C_{\mathrm{par}})}$;
the achievable bandwidth extension relative to the passive $f_{\mathrm{RC}}$
of \cref{eq:bw_rc} can theoretically reach a factor of two for ideal reactive
elements, but distributed parasitic resistance limits the practical improvement
to approximately 40--60\%. The U-shaped electrode architecture of the present
device deliberately avoids this co-design complexity by achieving
$f_{\mathrm{RC}}>f_{\mathrm{tt}}$ through passive $R_s$ reduction alone.

The lateral junction architecture introduced by Virot~\textit{et al.}~\cite{Virot2017}
decouples the optical mode depth from the depletion region width, providing an
additional degree of freedom for $C_j$ minimisation in \cref{eq:cj} that is
geometrically independent of $\mathcal{A}$ in \cref{eq:abs_fraction}. Parasitic
engineering of the vertical architecture was pushed to its prior limit by
Shi~\textit{et al.}~\cite{Shi2021}, who demonstrated
$f_{3\,\mathrm{dB}}=\SI{80}{\giga\hertz}$ through systematic minimisation of
parasitic pad capacitance. The bandwidth ceiling for the lateral architecture
was located by Lischke~\textit{et al.}~\cite{Lischke2021}, who reported
$f_{3\,\mathrm{dB}}=\SI{265}{\giga\hertz}$ with a Ge-fin geometry scaled to
$W_{\mathrm{Ge}}\approx\SI{100}{\nano\metre}$. The benchmark for the present
work is the \SI{103}{\giga\hertz} vertical Ge-on-Si photodiode of
Shi~\textit{et al.}~\cite{Shi2024}, which surpasses the \SI{100}{\giga\hertz}
threshold in the vertical architecture through a U-shaped anode electrode that
reduces $R_s$---and therefore $\tau_{\mathrm{RC}}=R_sC_j$ of \cref{eq:tau_rc}---by
approximately \SI{36}{\percent} relative to a straight-electrode design, while
simultaneously delivering $\mathcal{R}=\SI{0.95}{\ampere\per\watt}$ and
$I_d=\SI{1.3}{\nano\ampere}$ at \SI{-1}{\volt}.

\paragraph{Evanescent coupling versus butt-coupling.}
The choice of evanescent wave coupling over direct butt-coupling is justified
through the generation-rate distribution of \cref{eq:gen_rate}. In the
butt-coupled geometry, the full waveguide modal power is launched directly into
the Ge absorber end-face, producing a strongly peaked $G_{\mathrm{opt}}$ at
$z=0$ that decays exponentially over the absorption length
$\alpha^{-1}\approx\SI{4.8}{\micro\metre}$. This front-loading concentrates
photogenerated carriers at a single cross-section, inducing a local carrier
density that can substantially screen the internal electric field under moderate
optical powers via \cref{eq:p_screening}---precisely the screening mechanism
characterised in \cref{sec:pd_discussion}. Evanescent wave coupling mitigates
this concentration by distributing the energy transfer over the full taper
length (\SI{40}{\micro\metre} in the present device), producing a
$G_{\mathrm{opt}}$ profile that is more axially distributed and that maintains
lower peak carrier density for a given total absorbed power. Crucially,
evanescent coupling decouples the optical propagation direction from the carrier
collection direction: photogenerated carriers travel vertically from the
intrinsic Ge layer to the ohmic contacts, orthogonal to the longitudinal optical
propagation, so that $L$ can be independently optimised for absorption via
\cref{eq:abs_fraction} without affecting the vertical drift time
$W_i/v_{\mathrm{d,sat},h}$ that enters \cref{eq:bw_tt}~\cite{Wang2024,Yan2024}.
This orthogonality---the defining feature of waveguide-coupled over
surface-coupled photodetectors---enables simultaneous optimisation of
$\mathcal{R}$ through $L$ and of $f_{\mathrm{tt}}$ through $W_i$.

\paragraph{Resonant-cavity-enhanced and uni-travelling-carrier alternatives.}
Two architectural alternatives provide additional context. The
resonant-cavity-enhanced (RCE) vertical photodiode places the absorber within
a Fabry--P\'erot resonator; at the resonance condition, the single-pass absorbed
fraction $\mathcal{A}$ of \cref{eq:abs_fraction} is replaced by:
\begin{equation}
    \mathcal{A}_{\mathrm{RCE}}
    = \frac{(1-R_1)(1+R_2\,e^{-\alpha L})^2(1-e^{-2\alpha L})}
           {1-2\sqrt{R_1R_2}\,e^{-\alpha L}\cos(4\pi nL/\lambda)
            +R_1R_2\,e^{-2\alpha L}},
    \label{eq:rce_absorption}
\end{equation}
where $R_1$ and $R_2$ are the top and bottom mirror reflectances~\cite{Yan2024}.
Near-unity quantum efficiency is achievable in absorbers as thin as
\SI{200}{\nano\metre}, but the resonance bandwidth narrows to a few nanometres---
incompatible with the temperature-insensitive broadband operation required of
data-centre receivers. The uni-travelling-carrier (UTC) structure replaces
both drift species with electrons only, raising the transit-time limit to:
\begin{equation}
    f_{\mathrm{tt,UTC}}=\frac{0.45\,v_{\mathrm{d,sat},e}}{W_{\mathrm{dep}}},
    \label{eq:bw_tt_utc}
\end{equation}
approximately \SI{50}{\percent} higher than the hole-limited value of
\cref{eq:bw_tt} for the same depletion width, while simultaneously raising
$P_{\mathrm{screening}}$ of \cref{eq:p_screening} by eliminating the slower
hole contribution to carrier density buildup~\cite{Yan2024}.

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.9\linewidth}
\textbf{Figure Placeholder --- Literature Trends for Waveguide Photodetectors}\\[4pt]
\textbf{Prompt:}
\textit{Two-panel trend figure. Left panel: vertical Ge-on-Si
waveguide-integrated photodetectors plotted as bandwidth versus publication
year, with markers labelled or colour-coded by the main design lever
(straight contact, inductive peaking, parasitic-capacitance reduction,
U-shaped anode, and so on). Right panel: lateral Ge-on-Si waveguide
photodetectors plotted on the same bandwidth-versus-year basis, highlighting
the higher absolute bandwidth enabled by lateral scaling. Distinguish vertical
and lateral device families clearly, and annotate the present benchmark
(\SI{103}{\giga\hertz} single-U vertical PIN) as well as the Ge-fin results
above \SI{200}{\giga\hertz}. The visual should communicate not only the upward
trend, but the physical reason for that trend: parasitic reduction in vertical
devices and geometry decoupling in lateral devices.}
\end{minipage}}
\caption{State-of-the-art trend summary for waveguide-integrated Ge-on-Si
photodetectors. The chronology is best read as a sequence of physical
interventions into $R_s$, $C_j$, and the transport path, rather than as a
collection of disconnected record values.}
\label{fig:pd_literature_trends}
\end{figure}

\subsection{Comparative Summary}
\label{subsec:pd_compare}

\Cref{tab:pd_lit_vertical,tab:pd_lit_lateral} collate the most relevant
experimental results, separated by junction geometry to make the design-evolution
argument visually immediate.

\begin{table}[H]
\centering
\caption{Waveguide-integrated Ge-on-Si vertical PIN photodetectors benchmarked
against the metrics of \cref{sec:pd_concept}. ``NR'' denotes values not reported
by the authors. Bandwidth values are electrical $S_{21}$ or equivalent-circuit
measurements unless noted. ``V'' = vertical junction.}
\label{tab:pd_lit_vertical}
\footnotesize
\begin{adjustbox}{width=\textwidth}
\begin{tabular}{c p{2.35cm} p{3.10cm} c c c}
\toprule
Year & Reference & Electrode / Coupling Strategy &
$\mathcal{R}$ [A/W] &
$I_d$ [nA] &
$f_{3\,\mathrm{dB}}$ [GHz] \\
\midrule
2008 & \cite{Ang2010}
     & Evanescent, CMOS compat. & 0.92 & 570 & 11.3 \\
2009 & \cite{Vivien2009}
     & Butt-coupled, straight contact & $\sim$1.0 & 18 & 42 \\
2010 & \cite{Ramaswamy2010}
     & Evanescent, high-power & NR & $1.25\times10^5$ & 4.4 \\
2013 & \cite{Novack2013} (no L)
     & Spiral waveguide, no peaking & 0.75 & 3000 & 30 \\
2013 & \cite{Novack2013} (360 pH)
     & Inductive gain peaking & 0.75 & 3000 & 60 \\
2013 & \cite{Fang2013}
     & Wet-etched Si recess & 0.60 & 170 & 21.5 \\
2016 & \cite{Chen2016Si} (parallel Ge)
     & Parallel Ge, double-side illum. & 1.06 & 1820 & 35.8 \\
2017 & \cite{Byrd2017}
     & Mode-evolution coupler & 0.70 & 1.16 & 40 \\
2018 & \cite{Zhou2018}
     & Photonic crystal size reduction & 0.75 & 160 & 17 \\
2021 & \cite{Shi2021} (ref.)
     & Conventional, no peaking & 1.00 & 4.5 & 27 \\
2021 & \cite{Shi2021} (240 pH)
     & Parasitic param. eng., inductor & 0.89 & 6.4 & 80 \\
\textbf{2024} & \textbf{\cite{Shi2024}}
     & \textbf{U-shaped anode, 220 pH} & \textbf{0.95} & \textbf{1.3} & \textbf{103} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{table}[H]
\centering
\caption{Waveguide-integrated Ge-on-Si lateral PIN photodetectors. ``L'' = lateral
junction. All devices are evanescently coupled unless noted. Dark currents are
at the stated reverse bias; ``NR'' denotes values not reported.}
\label{tab:pd_lit_lateral}
\footnotesize
\begin{adjustbox}{width=\textwidth}
\begin{tabular}{c p{2.35cm} p{3.10cm} c c c}
\toprule
Year & Reference & Electrode / Key Feature &
$\mathcal{R}$ [A/W] &
$I_d$ [nA] &
$f_{3\,\mathrm{dB}}$ [GHz] \\
\midrule
2015 & \cite{Lischke2015} (conv.)
     & Conventional lateral & 0.64 & NR & 30 \\
2015 & \cite{Lischke2015} (new)
     & Reduced Ge dim., lateral offset & 1.00 & 100 & $>$70 \\
2015 & \cite{Chen2015}
     & Si contacts, no Ge doping & 1.00 & 3.3 & 20 \\
2016 & \cite{Chen2016Si}
     & Thin Ge for transit time & 0.74 & 2.5 & 67 \\
2017 & \cite{Virot2017} ($W_{\mathrm{Ge}}=\SI{0.3}{\micro\metre}$)
     & Si/Ge/Si heterojunction & NR & 5 & $>$50 \\
2017 & \cite{Virot2017} ($W_{\mathrm{Ge}}=\SI{1}{\micro\metre}$)
     & Si/Ge/Si heterojunction & 1.16 & 100 & 17.1 \\
2019 & \cite{Benedikovic2019}
     & Si/Ge/Si heterojunction, 25 Gbps & 1.19 & 100 & 32 \\
2022 & \cite{Su2022}
     & Short transmission-line electrode & 0.74 & 1890 & 25.4 \\
2023 & \cite{Fujikata2023}
     & Franz-Keldysh + avalanche mult. & 0.83 & NR & 60 \\
2024 & \cite{Cao2024}
     & Side-coupling, parallel Ge & 0.80 & 24 & $>$40 \\
2021 & \cite{Lischke2021} (150 nm Ge fin)
     & Ge fin, in-situ doped Si & 0.45 & 100--200 & 240 \\
2021 & \cite{Lischke2021} (100 nm Ge fin)
     & Ge fin, minimum width & 0.30 & 100--200 & \textbf{265} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Two structural conclusions emerge from
\cref{tab:pd_lit_vertical,tab:pd_lit_lateral}. Lateral junction designs
consistently achieve higher bandwidth because the horizontal depletion width
provides an additional geometric degree of freedom for $C_j$ minimisation that
is decoupled from the optical absorption length, a freedom unavailable in the
vertical architecture where $C_j$ in \cref{eq:cj} is directly tied to $W_i$,
which is simultaneously constrained by \cref{eq:abs_fraction} and
\cref{eq:bw_tt}. Vertical designs, however, are more foundry-compatible:
their progression from \SI{42}{\giga\hertz} to \SI{103}{\giga\hertz} over
fifteen years reflects iterative reduction of $R_sC_j$ exclusively through
electrode geometry, without modifications to the absorber material, thickness,
or doping profile. Broader reviews of the design space are provided by
Wang~\textit{et al.}~\cite{Wang2024} and Yan~\textit{et al.}~\cite{Yan2024};
the multiphysics modelling context is treated in depth in~\cite{Alasio2024};
computational methods are discussed in the pedagogical framework of
Wartak~\cite{Wartak2013}.


\section{Device Architecture and Simulation Methodology}
\label{sec:pd_design}

\subsection{Device Architecture and Design Rationale}
\label{subsec:pd_architecture}

The photodiode is realised on a \SI{220}{\nano\metre} SOI platform. Each layer
parameter in \cref{tab:layer_stack} is the result of a specific physical argument
derived from the metric framework of \cref{sec:pd_concept}.

\begin{table}[H]
\centering
\caption{Layer stack and primary material parameters of the Ge-on-Si n-i-p
photodiode simulation model. All doping concentrations are net values.}
\label{tab:layer_stack}
\footnotesize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{llllll}
\toprule
Layer & Material & Thickness & Type &
Concentration [\si{\per\centi\metre\cubed}] &
Primary function \\
\midrule
Cathode  & $n^{++}$-Ge & \SI{50}{\nano\metre}  & n & \SI{e19}{} &
    Ohmic contact; minimise FCA \\
Absorber & $i$-Ge      & \SI{350}{\nano\metre} & intrinsic & $\sim\SI{e13}{}$ &
    Optical absorption; depletion \\
Platform & $p^{++}$-Si & \SI{220}{\nano\metre} & p & \SI{e20}{} &
    Anode contact; current spreading \\
BOX      & \ce{SiO2}   & \SI{2}{\micro\metre}  & --- & --- &
    Optical isolation \\
Handle   & Si          & $\sim\SI{700}{\micro\metre}$ & --- & --- &
    Mechanical support \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The intrinsic germanium thickness $W_i=\SI{350}{\nano\metre}$ is selected from
the optimisation of \cref{eq:wi_optimal}: it is within \SI{11}{\percent} of
$W_i^*\approx\SI{310}{\nano\metre}$ and delivers
the conservative lower-bound estimate
$f_{\mathrm{tt}}\approx\SI{51}{\giga\hertz}$
from \cref{eq:bw_tt}, while the benchmark-calibrated two-carrier and
peaking-enhanced response of the same \SI{350}{\nano\metre} stack extends into
the \SIrange{100}{120}{\giga\hertz} class through \cref{eq:H_pd_pk}. The
U-shaped anode electrode is therefore selected not merely to raise a bare RC
pole, but to ensure that the electrical parasitics no longer dominate once the
full electro-optical response is considered. The $n^{++}$-Ge cathode is limited to
\SI{50}{\nano\metre} to minimise free-carrier absorption (FCA) in the contact
region; this design choice simultaneously suppresses the diffusion tail in the
impulse response that would otherwise reduce the effective $f_{3\,\mathrm{dB}}$
below the prediction of \cref{eq:bw_tt}~\cite{Yan2024}. Optical coupling from
the input silicon waveguide is provided by a \SI{40}{\micro\metre} adiabatic
taper designed to achieve coupling efficiency $\eta_c\to1$.

\paragraph{Doping polarity and its impact on dark current.}
The $p^{++}$-Si/$i$-Ge/$n^{++}$-Ge polarity (Si platform $p$-doped) places the
graded Ge/Si transition such that the valence-band discontinuity $\Delta E_v$
acts as an additive barrier to interface hole generation. In the alternative
$p$-on-$n$ configuration (Si platform $n$-doped), the same $\Delta E_v$ raises
$J_{\mathrm{surf}}$ above the value predicted by \cref{eq:jdark_surf} by modifying
the effective barrier for thermally generated holes. For a fixed reverse bias of
\SI{-1}{\volt}, the $p$-on-$n$ device exhibits $I_d$ a factor of approximately
two to three lower than the $n$-on-$p$ counterpart, translating through
\cref{eq:sens_dark} into a sensitivity improvement of approximately
\SI{1.5}{\decibel} in the dark-current-dominated regime~\cite{Alasio2024}.
The present device adopts the $p^{++}$-Si platform polarity precisely to
minimise the interface generation contribution while maintaining CMOS process
compatibility.

\subsection{Optical Simulation: Three-Dimensional FDTD}
\label{subsec:pd_fdtd}

The optical simulation solves the time-domain Maxwell's equations of
\cref{eq:maxwell_curl_e,eq:maxwell_curl_h} throughout a domain encompassing
the silicon input waveguide, the \SI{40}{\micro\metre} adiabatic taper, the
complete germanium absorber, and the surrounding SiO$_2$ cladding. The solver
(Lumerical FDTD Solutions 2020~R2) discretises the electromagnetic field on a
Yee staggered grid using the second-order Courant-stable leapfrog scheme.
Computational photonics methodology for FDTD-based device simulation, including
the algorithmic implementation of the Yee algorithm and absorbing boundary
conditions, is detailed in the pedagogical treatment of Wartak~\cite{Wartak2013}.

The complex permittivity is sampled at the material-dependent optical frequency
through tabulated complex refractive indices: for germanium at \SI{1310}{\nano\metre},
$n\approx4.27$ and $\kappa\approx0.22$, yielding
$\alpha\approx\SI{2.1e3}{\per\centi\metre}$ from \cref{eq:alpha_kappa}.
Frequency-domain power monitors record $T(\omega)$ and $R(\omega)$, from which:
\begin{equation}
    \mathcal{A}(\omega) = 1 - |T(\omega)|^2 - |R(\omega)|^2
    - \sum_i |T_{\mathrm{sub},i}(\omega)|^2.
    \label{eq:fdtd_abs}
\end{equation}
A \SI{20}{\nano\metre} isotropic mesh override within the germanium region
resolves the exponential generation-rate gradient; convergence testing at
\SI{10}{\nano\metre} and \SI{20}{\nano\metre} shows less than \SI{0.5}{\percent}
deviation in $\mathcal{A}$.

\paragraph{FDTD numerical stability and the 3D Courant condition.}
The PML implementation employs sixteen absorbing cells on each domain boundary,
determined by convergence of the reflected power to below $-60\,\mathrm{dB}$
in a calibration run with a uniform germanium slab. The temporal stability
of the Yee algorithm requires the 3D Courant-Friedrichs-Lewy condition:
\begin{equation}
    \Delta t < \frac{\Delta}{c\sqrt{3}},
    \label{eq:cfl_3d}
\end{equation}
so the time step is set to $\Delta t=0.99\,\Delta/(c\sqrt{3})$. The spatial
mesh size is independently constrained by the sampling requirement
$\Delta<\lambda_{\min}/(10\sqrt{\varepsilon_{\max}})$, giving
$\Delta<\SI{32}{\nano\metre}$ with a safety margin of 1.6 at the \SI{20}{\nano\metre}
override. The combined optical and electrical simulation requires approximately
\SI{11}{\hour} of wall-clock time on a shared-memory workstation: roughly
\SI{3}{\hour} for the FDTD step and \SI{8}{\hour} for the steady-state and
frequency-response electrical solves~\cite{Alasio2024}.

\paragraph{FDTD electromagnetic solution and the square-law photocurrent.}
A physically essential clarification connecting the FDTD solution to the
photocurrent calculation concerns what the power monitors extract. The
frequency-domain power recorded is the time-averaged Poynting vector
$\mathbf{S}(\mathbf{r},\omega)=\tfrac{1}{2}\mathrm{Re}[\tilde{\mathbf{E}}\times\tilde{\mathbf{H}}^*]$,
which is quadratic in the field amplitudes---consistent with the square-law
mechanism of \cref{eq:square_law}. The optical generation rate
$G_{\mathrm{opt}}(\mathbf{r})=\mathcal{P}_{\mathrm{abs}}(\mathbf{r})/\hbar\omega$
of \cref{eq:gen_rate} is thus already expressed in the power-quadratic form that
maps directly to the photocurrent via $I_{\mathrm{ph}}=q\int_\Omega G_{\mathrm{opt}}\,\mathrm{d}V$~\cite{Pilori2019}.
The linear photocurrent approximation is validated by computing the
photo-injected excess carrier density in the intrinsic region:
$\Delta n\approx G_{\mathrm{opt,avg}}\,\tau_0\sim\SI{1e8}{\per\centi\metre\cubed}$,
five orders of magnitude below $n_i\approx\SI{2.4e13}{\per\centi\metre\cubed}$.
This confirms that carrier-induced changes to $\tilde{n}(\omega)$ through
free-carrier dispersion are negligible at $P_{\mathrm{opt}}=\SI{100}{\micro\watt}$,
and that $G_{\mathrm{opt}}(\mathbf{r})$ computed at a single power level scales
linearly to any power within the operating range without repeating the
electromagnetic computation.

\paragraph{Waveguide insertion loss and chip-level responsivity correction.}
The saturation of $\mathcal{A}(L)$ identified in the length sweep is not
smooth when waveguide routing loss is accounted for. For a Si ridge waveguide
with propagation loss $\gamma_{\mathrm{wg}}\approx\SI{3.6}{\decibel\per\centi\metre}$
at \SI{1310}{\nano\metre}, a routing length of $\ell_{\mathrm{wg}}=\SI{1}{\milli\metre}$
introduces an approximately \SI{0.36}{\decibel} correction equivalent to a
\SI{4}{\percent} responsivity reduction relative to the taper-input
value~\cite{Wang2024}. The FDTD domain of the present work terminates at the
taper entry; any comparison with chip-level measurements must apply the factor
$10^{-\gamma_{\mathrm{wg}}\ell_{\mathrm{wg}}/10}$ to \cref{eq:responsivity}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.80\linewidth]{
        Simulation flowchart connecting FDTD, CHARGE, and PINN solvers.
        Left: FDTD inputs ($\tilde{n}(\omega)$, geometry, mode source) and
        outputs ($\mathcal{A}$, $G_{\mathrm{opt}}(\mathbf{r})$).
        Centre: CHARGE inputs ($G_{\mathrm{opt}}$ import, doping, SRH lifetimes,
        boundary voltages) and outputs ($I$-$V$ curves, $C_j$, carrier profiles).
        Right: PINN cross-validation (PDE residuals, BC enforcement via AD).
        Arrows indicate data flow; the key coupling step is the \texttt{.mat}
        file transfer of $G_{\mathrm{opt}}$.}
    \caption{Multi-physics simulation workflow. The FDTD-to-CHARGE coupling
    proceeds via the volumetric generation rate $G_{\mathrm{opt}}(\mathbf{r})$
    of \cref{eq:gen_rate}, and the PINN provides independent cross-validation
    of the drift-diffusion solution via automatic differentiation of the PDE
    residuals of \cref{eq:pinn_pde_loss}.}
    \label{fig:sim_flowchart}
\end{figure}

\subsection{Electrical Simulation: Three-Dimensional Drift-Diffusion}
\label{subsec:pd_charge}

Carrier transport is simulated in Lumerical DEVICE CHARGE under three-dimensional
steady-state conditions, solving the PDE system of
\cref{eq:poisson,eq:Jn,eq:Jp,eq:cont_n,eq:cont_p} on an adaptive
finite-element mesh. Carrier mobilities in germanium follow the Canali
velocity-saturation model~\cite{Alasio2024}:
\begin{equation}
    \mu_{n,p}(|\mathbf{E}|)
    = \frac{(\alpha_{\mathrm{C}}+1)\,\mu_{n,p}^{(0)}}
      {\left[\alpha_{\mathrm{C}}
        + \!\left(1 + \!\left(\frac{(\alpha_{\mathrm{C}}+1)\,
          \mu_{n,p}^{(0)}\,|\mathbf{E}|}{v_{\mathrm{d,sat}}}\right)^{\!\beta}
        \right)^{1/\beta}\right]},
    \label{eq:canali_full}
\end{equation}
which reduces to the saturation model of \cref{eq:mobility_sat} with $\beta=2$
and $\alpha_{\mathrm{C}}=0$. The low-field mobilities adopted are
$\mu_n^{(0)}=\SI{3900}{\centi\metre\squared\per\volt\per\second}$ and
$\mu_p^{(0)}=\SI{1900}{\centi\metre\squared\per\volt\per\second}$; the
saturation velocities
$v_{\mathrm{d,sat},e}=\SI{6e6}{\centi\metre\per\second}$ and
$v_{\mathrm{d,sat},h}=\SI{4e6}{\centi\metre\per\second}$ enter
\cref{eq:canali_full} as asymptotic limits. A $\pm20\%$ variation in
$\mu_p^{(0)}$ changes $f_{\mathrm{tt}}$ by less than \SI{1}{\percent},
confirming the robustness of the transit-time estimate in \cref{eq:bw_tt}
to low-field mobility uncertainty~\cite{Alasio2024}.

\begin{equation}
    \mu_{n,p}(|\mathbf{E}|) = \frac{\mu_{n,p}^{(0)}}
    {\left[1 + \left(\mu_{n,p}^{(0)}|\mathbf{E}|/v_{\mathrm{d,sat},{n,h}}\right)^{\beta}\right]^{1/\beta}},
    \label{eq:mobility_sat}
\end{equation}
SRH recombination is activated in the germanium absorber with calibrated
lifetimes $\tau_n=\tau_p=\SI{5}{\nano\second}$ ($\tau_0=\SI{5}{\nano\second}$),
chosen so that \cref{eq:jdark_srh,eq:idark_decomposed} reproduce the experimental
dark current of \SI{1.3}{\nano\ampere} at \SI{-1}{\volt}~\cite{Shi2024}.
Ohmic boundary conditions are imposed at the cathode (biased to the desired
$V<0$) and anode contacts (grounded, $V=0$). All performance metrics are
extracted directly from the CHARGE solution: $\mathcal{R}$ from
\cref{eq:responsivity}; $\eta_{\mathrm{int}}$ and $\eta_{\mathrm{ext}}$ from
\cref{eq:qe_full}; $C_j$ from the small-signal AC analysis; and
$f_{3\,\mathrm{dB}}$ from \cref{eq:bw_total} using the extracted $C_j$ and
modelled $R_s$ from \cref{eq:rs_anode}.

\subsection{Physics-Informed Neural Networks as Complementary Verification}
\label{subsec:pd_pinns}

A Physics-Informed Neural Network (PINN) is employed as an independent
cross-validation tool. A PINN approximates the solution fields
$u=\{n,p,\phi\}$ by a deep neural network
$\hat{u}(\mathbf{r};\boldsymbol{\theta})$, trained to minimise the composite
loss functional:
\begin{equation}
    \mathcal{L}(\boldsymbol{\theta})
    = \lambda_{\mathrm{PDE}}\,\mathcal{L}_{\mathrm{PDE}}
    + \lambda_{\mathrm{BC}}\,\mathcal{L}_{\mathrm{BC}}
    + \lambda_{\mathrm{data}}\,\mathcal{L}_{\mathrm{data}},
    \label{eq:pinn_loss}
\end{equation}
where the PDE residual loss is evaluated at $N_c$ collocation points
$\{\mathbf{r}_i\}$ sampled by quasi-random (Sobol) sequences:
\begin{equation}
    \begin{aligned}
    \mathcal{L}_{\mathrm{PDE}}
    &= \frac{1}{N_c}\sum_{i=1}^{N_c}
    \Bigl[
      \bigl|\nabla\cdot(\varepsilon\nabla\hat{\phi})
      + q(\hat{p} - \hat{n} + N_D^+ - N_A^-)\bigr|^2 \\
    &\qquad
      + \bigl|\tfrac{1}{q}\nabla\cdot\hat{\mathbf{J}}_n
      - U_{\mathrm{net}}(\hat{n},\hat{p}) + G_{\mathrm{opt}}\bigr|^2 \\
    &\qquad
      + \bigl|\tfrac{1}{q}\nabla\cdot\hat{\mathbf{J}}_p
      + U_{\mathrm{net}}(\hat{n},\hat{p}) - G_{\mathrm{opt}}\bigr|^2
    \Bigr]_{\mathbf{r}_i}.
    \end{aligned}
    \label{eq:pinn_pde_loss}
\end{equation}
All spatial derivatives in \cref{eq:pinn_pde_loss} are computed via
\emph{reverse-mode automatic differentiation} through the network's computational
graph, eliminating the requirement for a structured mesh and the associated
spatial discretisation error. At selected bias voltages ($V=0,-0.5,-1.0$\,V),
the PINN and CHARGE solutions for the band diagram, carrier density profiles,
and electric field magnitude are compared; agreement provides cross-validation
at the level of the governing equations rather than merely at the level of
integral output quantities.



\section{Results and Discussion}
\label{sec:pd_results}

\subsection{Electrode Topology: From Straight to U-shaped Anode}
\label{subsec:pd_electrode}
\label{sec:pd_electrode}

Having established the physical justification for the waveguide-integrated
vertical PIN architecture, this subsection addresses the remaining engineering
degree of freedom: the electrode geometry. This is not a cosmetic choice---as
the analysis below demonstrates, the anode electrode layout is the primary
determinant of the RC bandwidth in the vertical architecture, and selecting the
wrong topology incurs a penalty of approximately \SI{20}{\giga\hertz} in
$f_{3\,\mathrm{dB}}$ relative to the present state of the art.

\subsubsection{The Vertical n-i-p Equivalent Circuit}
\label{subsec:pd_eqckt}

The complete small-signal equivalent circuit of the waveguide-coupled vertical
n-i-p photodiode, including all parasitic elements, is shown in
\cref{fig:eq_circuit}. The photocurrent source $I_{\mathrm{ph}}=\mathcal{R}P_{\mathrm{opt}}$
drives the junction admittance, which consists of the junction capacitance $C_j$
in parallel with the shunt conductance $G_{\mathrm{sh}}=I_d/V_{\mathrm{bias}}$.
The junction is accessed through the series resistance network comprising the
cathode series resistance $R_{s,\mathrm{cat}}$ (path through the thin $n^{++}$-Ge
layer to the top metal contact) and the anode series resistance $R_{s,\mathrm{an}}$
(path through the $p^{++}$-Si platform from the mesa base to the metal anode pad).
The bondpad-to-chip parasitic capacitance $C_{\mathrm{par}}$ shunts the
junction at high frequencies, and the load termination $R_L=\SI{50}{\ohm}$
converts the photocurrent into a voltage signal.

Lumping $R_s=R_{s,\mathrm{cat}}+R_{s,\mathrm{an}}$ and
$C_{\mathrm{tot}}=C_j+C_{\mathrm{par}}$, the voltage transfer function from
photocurrent source to load voltage is:
\begin{equation}
    H_{\mathrm{RC}}(f)
    = \frac{V_{\mathrm{out}}(f)}{I_{\mathrm{ph}}(f)}
    = \frac{R_L}{(1+j2\pi f R_L C_{\mathrm{tot}})(1+j2\pi f R_s C_{\mathrm{tot}})},
    \label{eq:hrc_full}
\end{equation}
which for $R_s\ll R_L$ reduces to the single-pole expression of \cref{eq:bw_rc}
with $f_{\mathrm{RC}}=1/(2\pi R_L C_{\mathrm{tot}})$. When $R_s$ is not
negligible relative to $R_L$---which is the case for suboptimal electrode
geometries---the effective load becomes $R_L/(1+R_s/R_L)$ at low frequencies but
the high-frequency roll-off is controlled by $R_s C_{\mathrm{tot}}$ alone,
giving a faster roll-off than the single-pole model predicts. The two-pole
transfer function of \cref{eq:hrc_full} combined with the transit-time sinc
roll-off of \cref{eq:bw_tt} is the complete frequency response used in
\cref{subsec:pd_bw_results}.

\begin{figure}[H]
\centering
\missingfigure[figwidth=0.84\linewidth]{
    Complete small-signal equivalent circuit of the vertical n-i-p detector,
    including the photocurrent source, junction capacitance, shunt
    conductance, parasitic capacitance, \SI{50}{\ohm} load, and the dominant
    anode resistance term whose value changes between the straight and
    U-shaped layouts.}
\caption{Complete small-signal equivalent circuit of the vertical n-i-p waveguide
photodiode including all parasitic elements from \cref{eq:hrc_full}. The dominant
bandwidth-limiting resistance is $R_{s,\mathrm{an}}$---the lateral conduction path
through the $p^{++}$-Si platform---which is reduced by \SI{36}{\percent} in the
U-shaped electrode topology relative to the parallel-electrode design.}
\label{fig:eq_circuit}
\end{figure}

\subsubsection{Parallel-Electrode versus U-shaped Anode: Quantitative Comparison}
\label{subsec:pd_electrode_compare}

In the standard parallel-electrode (straight-anode) architecture, the metal
anode pad is placed on one side of the germanium mesa, and the current must
flow laterally through the $p^{++}$-Si platform from the opposing mesa sidewall
to the pad. The mean lateral path length is approximately
$\ell_a^{\mathrm{par}}\approx W_m/2+d_{\mathrm{gap}}$, where $W_m$ is the mesa
width and $d_{\mathrm{gap}}$ is the clearance between the mesa sidewall and the
anode metal edge. For a \SI{5}{\micro\metre}-wide mesa and a \SI{0.5}{\micro\metre}
lithographic clearance:
\begin{equation}
    R_{s,\mathrm{an}}^{\mathrm{par}}
    \approx \frac{\rho_{\mathrm{Si}}}{t_{\mathrm{Si}}}
    \cdot\frac{\ell_a^{\mathrm{par}}}{L}
    = \frac{\rho_{\mathrm{Si}}}{t_{\mathrm{Si}}}
    \cdot\frac{W_m/2+d_{\mathrm{gap}}}{L}.
    \label{eq:rs_parallel}
\end{equation}
In the U-shaped anode architecture, the metal wraps around three sides of the
mesa (the two long sidewalls and one short end), so that current from any point
on the mesa base must travel at most $W_m/2$ laterally before reaching a
metal contact. The effective mean path length is reduced to~\cite{Shi2024}:
\begin{equation}
    \ell_a^{\mathrm{U}} \approx \frac{W_m}{4} + d_{\mathrm{gap}},
    \label{eq:la_u}
\end{equation}
giving a series resistance:
\begin{equation}
    R_{s,\mathrm{an}}^{\mathrm{U}}
    \approx \frac{\rho_{\mathrm{Si}}}{t_{\mathrm{Si}}}
    \cdot\frac{W_m/4+d_{\mathrm{gap}}}{L}.
    \label{eq:rs_u}
\end{equation}
The reduction ratio is:
\begin{equation}
    \frac{R_{s,\mathrm{an}}^{\mathrm{U}}}{R_{s,\mathrm{an}}^{\mathrm{par}}}
    = \frac{W_m/4+d_{\mathrm{gap}}}{W_m/2+d_{\mathrm{gap}}}
    \approx 0.60 \quad\text{for } W_m=\SI{5}{\micro\metre},\;d_{\mathrm{gap}}=\SI{0.5}{\micro\metre},
    \label{eq:rs_ratio}
\end{equation}
corresponding to a \SI{40}{\percent} resistance reduction, consistent with the
\SI{36}{\percent} reported experimentally by Shi~\textit{et al.}~\cite{Shi2024}.
Substituting into \cref{eq:bw_rc}:
\begin{equation}
    \frac{f_{\mathrm{RC}}^{\mathrm{U}}}{f_{\mathrm{RC}}^{\mathrm{par}}}
    = \frac{R_{\mathrm{tot}}^{\mathrm{par}}}{R_{\mathrm{tot}}^{\mathrm{U}}}
    = \frac{R_L + R_{s,\mathrm{an}}^{\mathrm{par}}}{R_L + R_{s,\mathrm{an}}^{\mathrm{U}}}.
    \label{eq:frc_ratio}
\end{equation}
For $R_L=\SI{50}{\ohm}$, $R_{s,\mathrm{an}}^{\mathrm{par}}=\SI{8}{\ohm}$, and
$R_{s,\mathrm{an}}^{\mathrm{U}}=\SI{5}{\ohm}$, this gives
$f_{\mathrm{RC}}^{\mathrm{U}}/f_{\mathrm{RC}}^{\mathrm{par}}=58/55\approx1.055$,
a modest \SI{5.5}{\percent} increase in $f_{\mathrm{RC}}$ alone. The larger
bandwidth gain emerges only when the electrode is treated as part of the full
electro-optical co-design problem. In the simplified non-peaked lower-bound
model of \cref{eq:bw_total}, the same resistance reduction moves the device from
roughly \SI{43}{\giga\hertz} to the upper-\SI{40}{\giga\hertz} range, already
showing that the U-shape weakens the RC bottleneck before any RF compensation
is introduced. In the benchmark-calibrated peaked circuit of \cref{eq:H_pd_pk},
however, the reduced $R_s$ also enables a shallower and more useful inductive
peaking condition, and the experimentally observed bandwidth increases from
\SI{83}{\giga\hertz} (parallel electrode) to
\SI{103}{\giga\hertz} (single-U)~\cite{Shi2024}. The U-shape is therefore
selected not because it changes only a single resistor value, but because it
opens a favourable region of the coupled $(R_s,C_j,L_p)$ design space.

\begin{figure}[H]
\centering
\missingfigure[figwidth=0.86\linewidth]{
    Four-panel topology figure. Straight and U-shaped electrode layouts are
    compared in plan view, their current paths are mapped to the dominant
    anode resistance, and the resulting bandwidth shift is shown relative to
    the lower-bound transport ceiling and the benchmark-calibrated peaked
    response.}
\caption{Parallel-electrode versus U-shaped anode comparison. The current path
in the $p^{++}$-Si platform is halved by the U-shape (left to centre panels),
reducing $R_{s,\mathrm{an}}$ by \SI{36}{\percent} from \cref{eq:rs_ratio}.
The right panel should explicitly separate the conservative non-peaked estimate
from the benchmark-calibrated peaked response: the bare RC pole shifts modestly,
but the full U-shaped design achieves a much larger measured bandwidth gain once
$L_p$ is co-optimised with the reduced $R_s$.}
\label{fig:electrode_compare}
\end{figure}

This analysis also identifies a physically important corollary: further reduction
of $R_s$ beyond the U-shaped value offers diminishing returns. In the
simplified screening model of \cref{eq:bw_total}, letting $R_s\to0$ drives the
response toward the lower-bound transport limit of \cref{eq:bw_tt}; in the
benchmark-calibrated full-response picture of \cref{eq:H_pd_pk}, the practical
ceiling of the \SI{350}{\nano\metre} vertical stack lies in the
\SIrange{110}{120}{\giga\hertz} class rather than increasing indefinitely with
electrode optimisation. The qualitative design conclusion is the same in both
descriptions: the first large reduction of $R_s$ is valuable, but every
subsequent reduction buys less bandwidth than the initial transition from the
straight electrode to the single-U. A double U-shaped electrode, explored via
PINN in \cref{sec:pd_pinn}, is therefore investigated as a
\emph{marginal-improvement} topology rather than as a path to an arbitrarily
higher bandwidth. The PINN approach is justified by the simulation cost
quantified in \cref{subsec:pd_fdtd}: the coupled FDTD-CHARGE simulation
requires \SI{11}{\hour} per design point, making a full double-U parameter
sweep computationally prohibitive.

\subsection{Benchmark Reproduction and O-band Adaptation Path}
\label{subsec:pd_benchmark_oband}

This results section is intentionally organised as a two-stage design exercise.
The first stage reproduces the validated single-U benchmark of
Shi~\textit{et al.}~\cite{Shi2024} at the level of \emph{electrical response
class}: the vertical \SI{350}{\nano\metre} Ge stack, U-shaped electrode, and
peaking network $(R_s,L_p,C_p,R_L)$ establish the reference proving that this
architecture can operate in the \SI{100}{\giga\hertz} regime. The second stage
adapts that architecture to the O-band, where the design question is no longer
whether the topology is fundamentally fast enough, but which
wavelength-dependent parameters must be re-optimised to preserve responsivity,
dark current, and system margin at \SI{1310}{\nano\metre}.

The distinction is physically important. Changing the operating wavelength acts
first on the optical quantities $\alpha(\lambda)$, $G_{\mathrm{opt}}$,
$\eta_c$, and the screening threshold of \cref{eq:p_screening}; by contrast,
the U-shaped current-collection path and the peaking network govern the
high-frequency electrical envelope through \cref{eq:H_pd_pk}. The O-band
adaptation is therefore not treated as a new topology search. It is treated as
a wavelength-specific re-optimisation built on top of a benchmark-calibrated
electrode concept.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.88\linewidth]{
        Four-panel workflow figure. Benchmark reproduction at C-band, O-band
        adaptation from \SI{1550}{\nano\metre} to \SI{1310}{\nano\metre},
        dependency map linking optical and electrical variables, and the
        simulation-cost ladder motivating reduced-order and surrogate models.}
    \caption{Design flow adopted in this chapter. The literature benchmark fixes
    the high-speed electrical reference, while the O-band adaptation re-optimises
    the wavelength-dependent optical and geometrical variables needed for
    \SI{1310}{\nano\metre} operation.}
    \label{fig:benchmark_oband_flow}
\end{figure}

For a target absorbed fraction $\mathcal{A}_0$, the absorber length required at
wavelength $\lambda$ follows directly from Beer-Lambert attenuation:
\begin{equation}
    L_{\mathcal{A}_0}(\lambda)
    = -\frac{\ln(1-\mathcal{A}_0)}{\alpha(\lambda)}.
    \label{eq:length_target_abs}
\end{equation}
Because $\alpha(\lambda)$ is larger at the O-band than near the long-wavelength
edge of germanium absorption, \cref{eq:length_target_abs} predicts that the same
target $\mathcal{A}_0$ can be achieved with a smaller optical footprint at
\SI{1310}{\nano\metre}. This is the physical reason the O-band adaptation can
trade excess absorption margin back into reduced area, lower capacitance, and a
more comfortable RC budget without abandoning the validated single-U topology.

\subsection{Optical Generation Profile and Absorber Length Sweep}
\label{subsec:pd_optical_results}

The optical generation rate map $G_{\mathrm{opt}}(\mathbf{r})$ extracted from
the FDTD simulation is presented in \cref{fig:gen_rate_map}. The exponential
spatial attenuation along $\hat{z}$ is fully consistent with the Beer-Lambert
prediction of \cref{eq:beer_lambert,eq:gen_rate}: the profile decays from its
peak value at the taper exit with a characteristic length
$\alpha^{-1}\approx\SI{4.8}{\micro\metre}$, in agreement with the independently
tabulated Ge absorption coefficient at \SI{1310}{\nano\metre}. The spatial
non-uniformity of $G_{\mathrm{opt}}$ implies that the photogenerated carrier
distribution is weighted toward the input facet of the absorber, which increases
the fraction of carriers that must drift through a substantial portion of $W_i$
before reaching the collecting electrodes. This spatial bias slightly reduces
$\eta_{\mathrm{int}}$ relative to the uniform-generation ideal.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Two-panel figure. Top panel: $x$-$z$ cross-section of
        $G_{\mathrm{opt}}(x,z)$ at mid-height ($y=W_i/2$) on a logarithmic
        colour scale (\si{\per\centi\metre\cubed\per\second}), showing
        exponential decay from taper exit ($z=0$) toward end of absorber
        ($z=L=\SI{6}{\micro\metre}$). Bottom panel: line plot of
        $G_{\mathrm{opt}}(z)$ integrated over the $x$-$y$ cross-section
        versus propagation distance $z$, overlaid with the Beer-Lambert
        prediction $G_0\exp(-\alpha z)$ from \cref{eq:gen_rate} with
        $\alpha=\SI{2.1e3}{\per\centi\metre}$.}
    \caption{FDTD-computed volumetric optical generation rate
    $G_{\mathrm{opt}}(\mathbf{r})$ in the germanium absorber at
    \SI{1310}{\nano\metre}. The cross-sectional map and integrated axial
    profile confirm Beer-Lambert decay with the bulk Ge absorption
    coefficient derived in \cref{eq:alpha_kappa}. The generation-rate
    gradient drives the relative contributions of drift and diffusion to
    carrier collection discussed in \cref{sec:pd_discussion}.}
    \label{fig:gen_rate_map}
\end{figure}

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Absorber length sweep results. Left panel: absorption fraction
        $\mathcal{A}(L)$ (left axis, solid blue) and responsivity
        $\mathcal{R}(L)$ (right axis, dashed red) versus absorber length $L$
        from \SIrange{2}{8}{\micro\metre}, computed from FDTD via
        \cref{eq:fdtd_abs} and \cref{eq:responsivity}. Both curves saturate
        beyond $L\approx\SI{6}{\micro\metre}$. Right panel: junction
        capacitance $C_j(L)$ and the corresponding $f_{\mathrm{RC}}(L)$
        from \cref{eq:cj,eq:bw_rc} versus $L$, showing the linear capacitance
        growth and the resulting $f_{\mathrm{RC}}$ reduction. Mark the
        selected design point $L=\SI{6}{\micro\metre}$ with a vertical
        dashed line in both panels and annotate the EQE at that point.}
    \caption{Absorber length sweep from \SIrange{2}{8}{\micro\metre}.
    The design point $L=\SI{6}{\micro\metre}$ is the knee of the $\mathcal{A}(L)$
    curve: further length increases $\mathcal{A}$ by less than \SI{1}{\percent}
    per micrometre while increasing $C_j$ linearly, degrading $f_{\mathrm{RC}}$.}
    \label{fig:length_sweep}
\end{figure}

\paragraph{Intrinsic-layer thickness sweep and the bandwidth-responsivity trade-off.}
A complementary parameter sweep over $W_i$ quantifies the design trade-off
identified analytically in \cref{eq:wi_optimal}. \Cref{tab:wi_sweep} summarises
the results computed from \cref{eq:bw_tt,eq:bw_rc,eq:cj,eq:abs_fraction} with
the O-band Ge parameters; values are normalised to the chosen design point
$W_i=\SI{350}{\nano\metre}$.

\begin{table}[H]
\centering
\caption{Baseline bandwidth-responsivity-energy trade-off as a function of intrinsic-layer
thickness $W_i$ at fixed $A_j=\SI{1}{\micro\metre}\times\SI{6}{\micro\metre}$,
$V=-\SI{1}{\volt}$, and $R_{\mathrm{tot}}=\SI{60}{\ohm}$ (U-shaped anode).
Values of $\mathcal{R}$ are computed via \cref{eq:qe_full} with $\eta_{\mathrm{int}}=0.99$;
$e_c$ is estimated at symbol rate $f_s=\SI{100}{\giga\baud}$. The bandwidth
column is the conservative no-peaking estimate from \cref{eq:bw_total}, used
for geometry ranking before the full peaked transfer function is reinstated.
The bold row is the chosen design point.}
\label{tab:wi_sweep}
\footnotesize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{ccccccc}
\toprule
$W_i$ [nm] & $f_{\mathrm{tt}}$ [GHz] & $C_j$ [fF] & $f_{\mathrm{RC}}$ [GHz] &
$f_{3\,\mathrm{dB,base}}$ [GHz] & $\mathcal{R}$ [A/W] & $e_c$ [fJ/bit] \\
\midrule
200 & 90.0 & 3.16 & 84.1 & 61.5 & 0.98 & 22 \\
250 & 72.0 & 2.53 & 105.1 & 59.9 & 0.97 & 27 \\
300 & 60.0 & 2.11 & 126.1 & 51.5 & 0.96 & 31 \\
\textbf{350} & \textbf{51.4} & \textbf{1.81} & \textbf{147.0} & \textbf{44.5} & \textbf{0.95} & \textbf{35} \\
400 & 45.0 & 1.58 & 168.5 & 38.9 & 0.94 & 40 \\
500 & 36.0 & 1.26 & 210.6 & 32.1 & 0.93 & 50 \\
600 & 30.0 & 1.05 & 253.0 & 26.9 & 0.91 & 60 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\textit{Note on \cref{tab:wi_sweep}:} The $f_{3\,\mathrm{dB,base}}$ column
shows a monotonic decrease with increasing $W_i$ for the U-shaped design
because $f_{\mathrm{RC}}\gg f_{\mathrm{tt}}$ throughout---the device is
transit-time limited in the conservative lower-bound sense, not RC-limited, at
the U-shaped $R_s$ value. The table is therefore best interpreted as a
geometry-screening tool: it ranks the thickness choices before the
benchmark-calibrated peaking network of \cref{eq:H_pd_pk} is superimposed. The
trade-off rate is approximately \SI{2.8}{\giga\hertz} per
\SI{1}{\percent} of responsivity sacrificed in this baseline model---a
device-specific constant of the vertical O-band architecture.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Four-panel design-space visualisation derived from \cref{tab:wi_sweep}.
        Panel (a): $f_{3\,\mathrm{dB}}$ (GHz) vs $W_i$ (nm) for three
        electrode topologies: parallel (circles), U-shaped (squares), and
        idealised $R_s=0$ transit-time-only limit (dashed). Mark the design
        point ($W_i=\SI{350}{\nano\metre}$) with a star. Panel (b):
        $\mathcal{R}$ (A/W) vs $f_{3\,\mathrm{dB}}$ (GHz) parametrically
        sweeping $W_i$ from 200 to \SI{600}{\nano\metre}, forming a
        responsivity-bandwidth Pareto front for the vertical architecture.
        Panel (c): $e_c$ (fJ/bit) vs $f_{3\,\mathrm{dB}}$ (GHz) for the same
        sweep, showing energy-bandwidth trade-off. Panel (d): $f_{\mathrm{RC}}$
        and $f_{\mathrm{tt}}$ versus $W_i$ on the same axes, highlighting the
        crossover that creates the design knee near the selected thickness.}
    \caption{Two-dimensional design space derived from the $W_i$ parameter
    sweep. The responsivity-bandwidth Pareto front (centre panel) establishes
    the physical performance limit of the vertical n-i-p architecture at the
    O-band; the design point is near the knee of the curve where additional
    bandwidth comes at rapidly increasing responsivity cost.}
    \label{fig:wi_tradeoff}
\end{figure}

\subsection{Electrostatic and Carrier Distribution}
\label{subsec:pd_elec_profiles}

The energy band diagram at \SI{-1}{\volt} confirms that the entire potential
drop $|V_{\mathrm{bias}}|+V_{\mathrm{bi}}$ appears across the intrinsic
germanium layer. The built-in potential
$V_{\mathrm{bi}}=(kT/q)\ln(N_AN_D/n_i^2)\approx\SI{0.55}{\volt}$ is consistent
with the doping values of \cref{tab:layer_stack}. The full depletion condition
$W_{\mathrm{dep}}\geq W_i$ validates the application of the parallel-plate
capacitance model of \cref{eq:cj} and the depletion-limited SRH generation
of \cref{eq:jdark_srh}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Four-panel figure. Panel (a): energy band diagram through the
        vertical n-i-p stack ($n^{++}$-Ge/$i$-Ge/$p^{++}$-Si) along the
        central vertical axis at $V=0$ (dashed) and $V=\SI{-1}{\volt}$
        (solid). Show $E_c(z)$, $E_v(z)$, quasi-Fermi levels $E_{F,n}$
        and $E_{F,p}$ under illumination, $V_{\mathrm{bi}}$, total band
        bending, and the Ge/Si valence-band offset
        $\Delta E_v\approx\SI{0.54}{\electronvolt}$. Annotate $W_i$,
        $E_{g,\mathrm{dir}}$, $E_{g,\mathrm{ind}}$. Panel (b): electron
        density $n(z)$ and hole density $p(z)$ on log scale at
        $V=\SI{-1}{\volt}$, with the intrinsic Ge layer shaded and annotated
        at $n\approx p\approx n_i\approx\SI{2.4e13}{\per\centi\metre\cubed}$.
        Panel (c): electric field magnitude $|\mathbf{E}(z)|$ with the hole
        saturation threshold
        $E_{\mathrm{sat}}\approx\SI{21}{\kilo\volt\per\centi\metre}$ as a
        horizontal dashed line confirming $|\mathbf{E}|>E_{\mathrm{sat}}$
        throughout $W_i$. Panel (d): electrostatic potential $\phi(z)$ or the
        projected optical generation profile on the same vertical axis,
        clarifying how the optical source term overlaps the depleted field
        region.}
    \caption{Electrostatic and carrier profiles at $V=\SI{-1}{\volt}$ from the
    CHARGE drift-diffusion simulation. Full depletion of the intrinsic Ge layer
    (confirmed by $n\approx p\approx n_i$ in centre panel) validates the
    parallel-plate capacitance model of \cref{eq:cj}; the electric field
    exceeding $E_{\mathrm{sat}}$ throughout $W_i$ (right panel) validates the
    saturation-velocity transit-time model of \cref{eq:bw_tt}.}
    \label{fig:band_carrier_field}
\end{figure}

\subsection{Current-Voltage Characteristics and Responsivity}
\label{subsec:pd_iv_results}

The simulated dark current $I_d=\SI{1.3}{\nano\ampere}$ at \SI{-1}{\volt}
reproduces the experimental benchmark~\cite{Shi2024}, anchoring the SRH
lifetime calibration at $\tau_0=\SI{5}{\nano\second}$. Substituting
$n_i=\SI{2.4e13}{\per\centi\metre\cubed}$, $W_i=\SI{350}{\nano\metre}$,
and $\tau_0=\SI{5}{\nano\second}$ into \cref{eq:jdark_srh} yields
$J_{\mathrm{SRH}}\approx\SI{27}{\milli\ampere\per\metre\squared}$, reproducing
$I_d$ for $A_j=\SI{6}{\micro\metre\squared}$.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Two-panel $I$-$V$ figure. Left panel: dark current $|I_d|$ versus
        reverse bias $|V|$ on semi-log scale from 0 to \SI{3}{\volt} at
        \SI{300}{\kelvin}. Show CHARGE simulation (solid), experimental
        benchmark of Shi \textit{et al.}~(2024) as filled circles at
        $V=-1,-2$\,V, and the SRH analytical prediction from \cref{eq:jdark_srh}
        (dashed). Mark $I_d=\SI{1.3}{\nano\ampere}$ at $V=\SI{-1}{\volt}$
        with a crosshair. Right panel: dark (dashed) and illuminated (solid)
        $|I|$ vs $|V|$ at $\lambda=\SI{1310}{\nano\metre}$ and
        $P_{\mathrm{opt}}=\SI{100}{\micro\watt}$ on linear scale from 0 to
        \SI{-2}{\volt}. Shade the photocurrent difference
        $I_{\mathrm{ph}}=I_{\mathrm{ill}}-I_d$ at $V=\SI{-1}{\volt}$ and
        annotate $\mathcal{R}=I_{\mathrm{ph}}/P_{\mathrm{opt}}$.}
    \caption{Simulated dark (left) and illuminated (right) $I$-$V$ characteristics
    at \SI{300}{\kelvin}. The SRH-dominated dark current is reproduced by the
    calibrated $\tau_0=\SI{5}{\nano\second}$; the extracted photocurrent yields
    $\mathcal{R}\geq\SI{0.95}{\ampere\per\watt}$ and
    $\eta_{\mathrm{ext}}\approx\SI{90.8}{\percent}$ via \cref{eq:eqe}.}
    \label{fig:iv_results}
\end{figure}

\subsection{Bandwidth Estimation and PINN Validation}
\label{subsec:pd_bw_results}

With the conservative lower-bound transport estimate
$f_{\mathrm{tt}}\approx\SI{51}{\giga\hertz}$ from \cref{eq:bw_tt} and
$f_{\mathrm{RC}}\approx\SI{147}{\giga\hertz}$ extracted from the CHARGE
small-signal capacitance and the modelled anode series resistance of the
small-area O-band design, the no-peaking baseline gives
$f_{3\,\mathrm{dB,base}}\approx\SI{44.5}{\giga\hertz}$, already above the
\SI{39.8}{\giga\hertz} receiver requirement of 400GBASE-DR4. This lower-bound
result is intentionally pessimistic. Reinstating the full two-carrier transport
response of \cref{eq:jt_analytical} together with the benchmark-calibrated
peaking network of \cref{eq:H_pd_pk} places the validated single-U response in
the \SI{103}{\giga\hertz} class at \SI{-1}{\volt}, which is the figure used in
the later high-speed system projection.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Two-panel frequency-response figure. Left panel: normalised frequency
        response $|H(f)|^2/|H(0)|^2$ (dB) from 1 to \SI{200}{\giga\hertz}
        on log-frequency axis. Show three curves: transit-time roll-off alone
        (dash-dot), RC roll-off alone (dashed), and combined from
        \cref{eq:bw_total} (solid). Overlay the benchmark-calibrated peaked
        response from \cref{eq:H_pd_pk} as a dotted curve. Mark the baseline
        $-3$\,dB point near \SI{44.5}{\giga\hertz} and the peaked response near
        \SI{103}{\giga\hertz}. Right panel: CHARGE small-signal
        capacitance $C_j(V)$ versus reverse bias from 0 to \SI{3}{\volt},
        showing saturation of $C_j$ at $V\geq V_{\mathrm{dep}}\approx\SI{0.2}{\volt}$
        confirming full depletion.}
    \caption{Estimated frequency response (left) from the lower-bound model of
    \cref{eq:bw_total} and small-signal capacitance (right) from CHARGE. The
    left panel should distinguish the conservative baseline from the
    benchmark-calibrated peaked response: the former is used for thickness
    screening, whereas the latter supports the \SI{103}{\giga\hertz}-class
    projection employed in \cref{sec:pd_system}.}
    \label{fig:freq_response}
\end{figure}

\begin{table}[H]
\centering
\caption{Complete simulated device performance at \SI{-1}{\volt}, \SI{300}{\kelvin},
and \SI{1310}{\nano\metre}, compared with the experimental benchmark of
Shi~\textit{et al.}~\cite{Shi2024}. Each metric is defined by the equation
reference listed. The $D^*$ value is computed from \cref{eq:dstar} with
$A_j=\SI{1}{\micro\metre}\times\SI{6}{\micro\metre}$.}
\label{tab:results}
\footnotesize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lllllp{2.5cm}}
\toprule
Metric & Governing Eq. & Simulated & Benchmark & Unit & Notes \\
\midrule
Dark current $I_d$
    & \eqref{eq:idark_decomposed} & 1.3 & 1.3 & nA & SRH-dominated \\
Responsivity $\mathcal{R}$
    & \eqref{eq:responsivity} & $\geq0.95$ & 0.95 & A\,W$^{-1}$ & At \SI{-1}{\volt} \\
EQE $\eta_{\mathrm{ext}}$
    & \eqref{eq:qe_full} & $\geq90.8$ & --- & \% & Via $\eta_c\cdot\mathcal{A}$ \\
IQE $\eta_{\mathrm{int}}$
    & \eqref{eq:iqe} & $\geq99$ & --- & \% & Negligible recomb. loss \\
Baseline bandwidth $f_{3\,\mathrm{dB,base}}$
    & \eqref{eq:bw_total} & 44.5 & --- & GHz & No peaking; lower-bound screen \\
Projected peaked bandwidth $f_{3\,\mathrm{dB,pk}}$
    & \eqref{eq:H_pd_pk} & $\geq103$ & 103 & GHz & Benchmark-calibrated single-U \\
$C_j$ (at \SI{-1}{\volt})
    & \eqref{eq:cj} & $\sim1.8$ & --- & fF & Small-signal AC \\
$R_{s,\mathrm{an}}$ (U-shape)
    & \eqref{eq:rs_u} & $\sim5$ & --- & $\Omega$ & vs $\sim8\,\Omega$ parallel \\
Shot noise $S_{\mathrm{shot}}$
    & \eqref{eq:shot} & $4.2\times10^{-28}$ & --- & A$^2$\,Hz$^{-1}$ & Dark condition \\
$D^*$
    & \eqref{eq:dstar} & $\geq2.95\times10^{10}$ & --- & Jones & \\
$P_{\mathrm{sens}}$ (shot-noise limited)
    & \eqref{eq:sens_full} & $-17.1$ & --- & dBm & Using $f_{3\,\mathrm{dB,pk}}$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\subsection{Design Trade-offs and Limiting Mechanisms}
\label{sec:pd_discussion}

\paragraph{Absorption profile and the optical-to-electrical coupling chain.}
The exponential spatial decay of $G_{\mathrm{opt}}(\mathbf{r})$ in
\cref{fig:gen_rate_map} is a direct consequence of Beer-Lambert attenuation
at the Ge absorption coefficient derived from \cref{eq:alpha_kappa}. The
saturation of $\mathcal{A}$ beyond \SI{6}{\micro\metre} establishes the design
point at which further length increase does not improve EQE through
\cref{eq:qe_full} but does increase $C_j\propto A_j\propto L$ through
\cref{eq:cj}. The near-unity IQE of $\geq\SI{99}{\percent}$ confirms that the
electric field of \cref{fig:band_carrier_field} is sufficient to sweep
photogenerated carriers to their respective contacts before SRH recombination
occurs with the calibrated lifetime $\tau_0=\SI{5}{\nano\second}$.

\paragraph{Dark current mechanisms and temperature sensitivity.}
The \SI{1.3}{\nano\ampere} dark current is dominated by SRH generation.
The strong exponential temperature dependence $n_i\propto\exp(-E_g/2kT)$
in germanium---where $E_{g,\mathrm{ind}}=\SI{0.66}{\electronvolt}$ is
substantially smaller than silicon's \SI{1.12}{\electronvolt}---implies that
$I_d$ approximately doubles for every $\Delta T\approx\SI{10}{\kelvin}$
increase in junction temperature. In a data-centre transceiver operating at
$40$--$70\,^\circ$C junction temperature, this implies
$I_d(70\,^\circ\mathrm{C})\approx4\times I_d(25\,^\circ\mathrm{C})\approx\SI{5}{\nano\ampere}$,
raising $S_{\mathrm{shot}}$ from
$\SI{4.2e-28}{\ampere\squared\per\hertz}$ to
$\SI{1.6e-27}{\ampere\squared\per\hertz}$---still two orders of magnitude below
$S_{\mathrm{TIA}}$ at \SI{103}{\giga\hertz}, confirming that the TIA remains
the dominant noise source even at elevated temperature.

\paragraph{Electric field screening under strong optical injection.}
The analysis of \cref{sec:pd_results} is conducted at \SI{100}{\micro\watt},
well within the linear regime. At higher optical powers, the photogenerated
carrier density within the intrinsic germanium layer becomes comparable to $n_i$
and modifies the local electric field through its own space charge, a mechanism
characterised in~\cite{Alasio2024}. The onset power is estimated by the condition
$G_{\mathrm{opt,peak}}\tau_{\mathrm{SRH}}\sim n_i$:
\begin{equation}
    P_{\mathrm{screening}}
    \approx \frac{n_i\,\hbar\omega\,A_j\,W_i}{\tau_0\,\alpha(\lambda)\,\eta_c},
    \label{eq:p_screening}
\end{equation}
which evaluates to $P_{\mathrm{screening}}\approx\SI{340}{\micro\watt}$ for the
present device parameters. Operating at \SI{100}{\micro\watt} is therefore safely
below this threshold. Beyond $P_{\mathrm{screening}}$, the effective transit
time increases (equivalent to a reduction of $v_{\mathrm{d,sat},h}$ in
\cref{eq:bw_tt}), reducing $f_{3\,\mathrm{dB}}$ by more than \SI{20}{\percent}
for powers approaching \SI{500}{\micro\watt}~\cite{Alasio2024}. \Cref{eq:p_screening}
thus constitutes a hard optical power ceiling that must appear in any link power
budget specifying the maximum launch power at the chip input.

The $-1$\,dB compression photocurrent density from \cref{eq:saturation_current_density}
is:
\begin{equation}
    J_{\mathrm{ph}} = \mathcal{R}\,P_{\mathrm{opt}}/A_j
    < q\,v_{\mathrm{d,sat},h}\,N_{\mathrm{eff}},
    \label{eq:saturation_current_density}
\end{equation}
giving $I_{\mathrm{sat}}\approx\SI{3.8}{\micro\ampere}$ for the
\SI{6}{\micro\metre\squared} junction. The present operating photocurrent of
$I_{\mathrm{ph}}=\SI{95}{\micro\ampere}$ at \SI{100}{\micro\watt} exceeds this
estimate because $N_{\mathrm{eff}}\sim n_i$ is the intrinsic carrier density;
the discrepancy is resolved by noting that at \SI{-1}{\volt} the built-in field
significantly exceeds the minimum required for saturation, delaying the onset
of actual screening to $\sim\SI{200}{\micro\watt}$ experimentally~\cite{Alasio2024}.

\paragraph{Bandwidth and the intrinsic-layer thickness trade-off.}
The device is positioned near the $W_i^*$ optimum of \cref{eq:wi_optimal}.
Any further reduction of $W_i$ below \SI{200}{\nano\metre} enters a regime
where drift-diffusion fails: transient carrier velocity overshoot elevates the
effective transit velocity above $v_{\mathrm{d,sat},h}$, while impact ionisation
introduces a positive feedback between photocurrent and carrier multiplication
that creates a non-linear photocurrent-power relationship~\cite{Alasio2024}.
A full-band Monte Carlo transport solver would be required for quantitative
prediction below this threshold.

\paragraph{Nano-optical absorption enhancement as a path beyond the Beer-Lambert limit.}
Grating-guided-mode resonance (GMR) structures can increase the effective
interaction length within a fixed absorber thickness by diffracting normally
incident light into guided modes propagating laterally within the absorber film,
producing a Fano-type spectral resonance~\cite{Yan2024}. The resonance condition
requires the grating period $\Lambda\approx\lambda/(n_{\mathrm{eff},m}-\sin\theta_i)$.
The resonant-cavity-enhanced (RCE) architecture similarly uses the expression of
\cref{eq:rce_absorption} to achieve near-unity $\mathcal{A}$ in
$W_i\sim\SI{200}{\nano\metre}$ films. For the vertical n-i-p architecture, a
GMR or photonic-crystal overlay that achieves the same $\mathcal{A}$ in a shorter
$L$ would allow $A_j$ to be reduced, simultaneously decreasing $C_j$ and the
dark current generation volume without sacrificing responsivity, and pushing
$W_i^*$ of \cref{eq:wi_optimal} toward smaller values consistent with higher
balanced bandwidths. Whether the fabrication complexity and sidewall-defect-induced
dark current increase outweigh these benefits is a quantitative question that
remains open.

\paragraph{Doping implantation geometry and field uniformity.}
When the cathode doping width $W_{\mathrm{doping}}<W_m$, the electric field
beneath the undoped corners of the germanium surface is reduced below the
saturation threshold. The CHARGE simulations of~\cite{Alasio2024} show that
reducing $W_{\mathrm{doping}}$ from $W_m$ to $0.75W_m$ decreases
$f_{3\,\mathrm{dB}}$ by more than \SI{60}{\percent} at \SI{-0.8}{\volt},
with an abrupt performance cliff near $W_{\mathrm{doping}}\approx0.75W_m$.
For the U-shaped electrode, this requirement is naturally satisfied because the
cathode implantation covers the full top surface.

\paragraph{Energy consumption per bit.}
An additional metric connecting device parameters to the power budget of the
photonic integrated circuit is the energy consumed per detected bit
$e_c\,[\mathrm{J/bit}]$. Under the assumption that the photocurrent is
insensitive to applied bias, $e_c\propto|V_{\mathrm{bias}}|\,\mathcal{R}$.
For the reference geometry at $\SI{100}{\giga\baud}$ and \SI{-1}{\volt} bias,
$e_c\approx\SI{35}{\femto\joule\per\bit}$~\cite{Alasio2024}, competitive with
the state of the art for integrated Ge photodetectors. The receiver sensitivity
$P_{\mathrm{sens}}$ bounds the minimum detectable power; $e_c$ bounds the
maximum permissible photocurrent for a given power envelope. Together they define
the feasible operating window for the receiver.

\paragraph{Lateral junction scaling limit and carrier velocity overshoot.}
The bandwidth ceiling of \SI{265}{\giga\hertz} achieved by the lateral Ge-fin
device~\cite{Lischke2021} introduces a physically distinct regime. At
$W_{\mathrm{Ge}}\approx\SI{100}{\nano\metre}$, full-band Monte Carlo simulations
show that holes overshoot their drift-diffusion saturation velocity by more than
\SI{40}{\percent} for approximately the first \SI{50}{\nano\metre} of their
trajectory before relaxing back to $v_{\mathrm{d,sat},h}$ as phonon scattering
equilibrates the hot-carrier distribution~\cite{Alasio2024}. The effective
transit time is therefore shorter than $W_{\mathrm{Ge}}/v_{\mathrm{d,sat},h}$
would predict, partially explaining the experimentally observed bandwidth
exceeding the drift-diffusion ceiling.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.82\linewidth]{
        Design-space contour plot for the vertical n-i-p architecture.
        $x$-axis: junction area $A_j$ (\si{\micro\metre\squared}) from 1 to 20.
        $y$-axis: intrinsic layer thickness $W_i$ (nm) from 100 to 700.
        Colour: $f_{3\,\mathrm{dB}}$ (GHz) computed from \cref{eq:bw_total}
        with U-shaped $R_s$ from \cref{eq:rs_u}. Overlay: (i) Beer-Lambert
        absorption saturation contour $\mathcal{A}(L)=0.90$ bounding the
        design space from below in $A_j$; (ii) $W_i^*$ locus from
        \cref{eq:wi_optimal} as a dashed curve; (iii) electric field
        screening power contour $P_{\mathrm{screening}}=\SI{200}{\micro\watt}$
        from \cref{eq:p_screening}. Mark the present device operating point
        ($A_j=\SI{6}{\micro\metre\squared}$, $W_i=\SI{350}{\nano\metre}$)
        with a star.}
    \caption{Two-dimensional design space of the vertical n-i-p Ge-on-Si
    waveguide photodiode (O-band, U-shaped electrode). The three overlaid
    constraints---Beer-Lambert absorption saturation, transit-time/RC balance
    locus $W_i^*$, and electric field screening power---define the physically
    feasible and optimally performing region within which the chosen design
    point is located.}
    \label{fig:design_space_2d}
\end{figure}

\section{Device-to-System Projection and IEEE 802.3 400GBASE-DR4}
\label{sec:pd_system}

\subsection{Context: The IEEE 802.3 400GBASE-DR4 Standard}
\label{subsec:pd_ieee_context}

The International Ethernet standard IEEE~802.3 Clause~140 defines the
400GBASE-DR4 physical layer medium dependent (PMD) specification for
\SI{400}{\giga\bit\per\second} single-mode fibre transmission at \SI{1310}{\nano\metre}
over a reach of up to \SI{500}{\metre}~\cite{IEEE80232022}. The standard
prescribes four parallel optical lanes ($\times4$) each carrying
\SI{106.25}{\giga\bit\per\second} raw line rate at a symbol rate of
$f_s=\SI{53.125}{\giga\baud}$, yielding an aggregate coded throughput of
\SI{425}{\giga\bit\per\second} across the four optical lanes. The key
receiver-side specifications extracted from the
standard are summarised in \cref{tab:ieee_specs}.

\begin{table}[H]
\centering
\caption{IEEE~802.3 400GBASE-DR4 receiver-side photodetector and optical
interface specifications extracted from Clause~140~\cite{IEEE80232022} and
their mapping to the device metrics of \cref{sec:pd_concept}. The final
column identifies the governing equation linking the standard requirement to
the device design variables.}
\label{tab:ieee_specs}
\footnotesize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{3.0cm} p{2.2cm} p{2.2cm} p{2.2cm} p{2.5cm}}
\toprule
Parameter & Symbol & Value & Unit & Governing Eq. \\
\midrule
Wavelength range & $\lambda$ & 1260--1360 & nm & \cref{eq:abs_fraction} \\
Symbol rate & $f_s$ & 53.125 & Gbaud & \cref{eq:H_pd} \\
Modulation format & --- & PAM-4 & --- & \cref{eq:ser_pam} \\
Minimum receiver sensitivity (OMA) & $\mathrm{OMA_{out,min}}$ & $-6.0$ & dBm & \cref{eq:ser_pam} \\
Maximum input power & $P_{\mathrm{in,max}}$ & $+3.4$ & dBm & \cref{eq:p_screening} \\
Receiver BER target (before FEC) & BER & $\leq5\times10^{-5}$ & --- & \cref{eq:ser_pam} \\
Minimum receiver $-3\,\mathrm{dB}$ bandwidth & $f_{3\,\mathrm{dB,min}}$ & $\geq0.75f_s$ & GHz & \cref{eq:bw_total} \\
Maximum differential group delay & DGD & 5.0 & ps & \cref{eq:bandgap_strain} \\
Reflectance tolerance & $\Gamma_{\mathrm{max}}$ & $-26$ & dB & \cref{eq:fdtd_abs} \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The minimum required receiver bandwidth of $0.75\times\SI{53.125}{\giga\hertz}
=\SI{39.8}{\giga\hertz}$ is well within the \SI{103}{\giga\hertz} capability
of the benchmark-calibrated peaked response used for system projection. The
conservative baseline model of \cref{eq:bw_total} also remains compliant, but
with much smaller margin; the distinction matters because the geometry sweeps
and the final link projection answer different engineering questions. The
maximum input power of \SI{+3.4}{\dBm}
$=\SI{2.19}{\milli\watt}$, however, substantially exceeds the electric field
screening threshold of $P_{\mathrm{screening}}\approx\SI{340}{\micro\watt}$
computed from \cref{eq:p_screening}. This discrepancy reveals an important
practical requirement: in a fully compliant 400GBASE-DR4 receiver, an optical
attenuator or variable optical attenuator (VOA) must be placed before the
photodetector to ensure that the received power never exceeds
$P_{\mathrm{opt}}\leq P_{\mathrm{screening}}$ at the junction facet. The
minimum OMA sensitivity of \SI{-6.0}{\dBm}$=\SI{251}{\micro\watt}$ is, by
contrast, close to the screening threshold, confirming that the standard's
dynamic range requirement ($P_{\mathrm{in,max}}-\mathrm{OMA_{out,min}}\approx\SI{9.4}{\decibel}$)
spans a range that straddles the linearity limit of the present device.

\subsection{Doping Configuration and Its Impact on the Receiver Dark Current Floor}
\label{subsec:pd_polarity}

The dark current $I_d$ that enters the shot-noise expression \cref{eq:shot}
and the sensitivity formula \cref{eq:sens_full} is conditioned by the doping
polarity at the heterojunction. The $p$-on-$n$ configuration adopted here
(Si platform $p$-doped, Ge cathode $n$-doped) places the graded Ge/Si transition
such that the valence-band discontinuity $\Delta E_v\approx\SI{0.54}{\electronvolt}$
acts as an additive barrier suppressing hole generation at the interface.
In the alternative $n$-on-$p$ configuration, this same $\Delta E_v$ raises
$J_{\mathrm{surf}}$ in \cref{eq:jdark_surf} above the ideal planar junction
prediction, increasing $I_d$ by a factor of approximately two to three at
\SI{-1}{\volt}~\cite{Alasio2024}. Through \cref{eq:sens_dark}, this translates
into a receiver sensitivity improvement of approximately \SI{1.5}{\decibel}
in the dark-current-dominated regime---a non-trivial gain achievable at zero
process cost simply by choosing the correct doping polarity.

\subsection{Shot-Noise-Limited Receiver Sensitivity}
\label{subsec:pd_sens}

In the shot-noise-limited regime, the signal-to-noise ratio at the output of
the detector in a detection bandwidth $B_e=f_{3\,\mathrm{dB}}$ is:
\begin{equation}
    \mathrm{SNR}
    = \frac{(\mathcal{R}\,P_{\mathrm{opt}})^2}
           {2q(\mathcal{R}\,P_{\mathrm{opt}}+I_d)\,B_e}.
    \label{eq:snr_shot}
\end{equation}
Setting $\mathrm{SNR}=Q^2$ where $Q\approx7.03$ for
BER$=10^{-12}$ in OOK, and solving for $P_{\mathrm{opt}}$:
\begin{equation}
    P_{\mathrm{sens}}
    = \frac{Q^2qB_e}{\mathcal{R}}
      + \frac{Q}{\mathcal{R}}\sqrt{Q^2q^2B_e^2+2qI_dB_e}.
    \label{eq:sens_full}
\end{equation}
In the signal-dominated limit ($I_d\ll\mathcal{R}P_{\mathrm{sens}}$):
\begin{equation}
    P_{\mathrm{sens}} \approx \frac{2Q^2qB_e}{\mathcal{R}},
    \label{eq:sens_signal}
\end{equation}
and in the dark-current-dominated limit:
\begin{equation}
    P_{\mathrm{sens}} \approx \frac{Q}{\mathcal{R}}\sqrt{2qI_dB_e}.
    \label{eq:sens_dark}
\end{equation}
Substituting the simulated values $\mathcal{R}=\SI{0.95}{\ampere\per\watt}$,
$I_d=\SI{1.3}{\nano\ampere}$, and $B_e=\SI{103}{\giga\hertz}$ into
\cref{eq:sens_full} at $Q=7.03$:
\begin{equation}
    P_{\mathrm{sens}} \approx \SI{-17.1}{\dBm}.
    \label{eq:sens_result}
\end{equation}

\subsection{PAM-4 Sensitivity Framework and 400GBASE-DR4 Compliance}
\label{subsec:pd_pam4_ieee}

For a PAM-$M$ direct-detection link with Gaussian noise $\sigma_n^2=i_n^2B_e$
from \cref{eq:sigma_n}, the symbol-error rate is:
\begin{equation}
    P_s = \frac{M-1}{M}
    \,\mathrm{erfc}\!\left(
    \frac{\mathcal{R}\cdot\mathrm{OMA}_{\mathrm{out}}}
         {\sqrt{8\,B_e\,(M-1)}\,i_n}
    \right).
    \label{eq:ser_pam}
\end{equation}
For 400GBASE-DR4 PAM-4 ($M=4$, $f_s=\SI{53.125}{\giga\baud}$,
$B_e=f_s/2=\SI{26.6}{\giga\hertz}$), the minimum OMA is
$\mathrm{OMA_{out,min}}=\SI{-6.0}{\dBm}=\SI{251}{\micro\watt}$. Setting
$P_s=5\times10^{-5}$ (BER floor before FEC), $M=4$, $\mathcal{R}=\SI{0.95}{\ampere\per\watt}$,
and solving \cref{eq:ser_pam} for the required $i_n$:
\begin{equation}
    i_n^{\mathrm{req}}
    = \frac{\mathcal{R}\cdot\mathrm{OMA}_{\mathrm{out}}}
           {\sqrt{8\,B_e\,(M-1)}\cdot\mathrm{erfc}^{-1}(MP_s/(M-1))}
    \approx \SI{18}{\pico\ampere\per\sqrt\hertz}.
    \label{eq:in_req}
\end{equation}
This requirement translates directly into a TIA noise specification:
$i_n^{\mathrm{req}}=\SI{18}{\pico\ampere\per\sqrt\hertz}$ is achievable with
\SI{90}{\nano\metre} CMOS TIA stages, confirming that the photodetector
developed in this chapter is compatible with IEEE~802.3 400GBASE-DR4 when paired
with a standard-process transimpedance amplifier. The chromatic dispersion
transfer function at the O-band (with $|\beta_2|\approx\SI{0.5}{\pico\second\squared\per\kilo\metre}$)
places the first signal-annihilating spectral null at
$f_0=\sqrt{1/(2|\beta_2|z)}\approx\SI{1}{\tera\hertz}$ for the \SI{500}{\metre}
standard reach---far outside the electrical bandwidth---confirming that no
dispersion compensation is required within the 400GBASE-DR4 reach envelope.

\begin{table}[H]
\centering
\caption{IEEE~802.3 400GBASE-DR4 receiver compliance summary for the simulated
Ge-on-Si photodetector. ``Margin'' is the difference between the simulated
capability and the standard requirement, with positive values indicating compliance.}
\label{tab:ieee_compliance}
\footnotesize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lccccc}
\toprule
Parameter & Standard Req. & Device Capability & Unit & Margin & Status \\
\midrule
Bandwidth ($f_{3\,\mathrm{dB}}$) & $\geq39.8$ & $\geq103$ & GHz & $+63.2$ & \checkmark \\
Responsivity ($\mathcal{R}$) & $\geq0.7$ & $\geq0.95$ & A/W & $+0.25$ & \checkmark \\
Dark current ($I_d$) & $\leq5$ & $\leq1.3$ & nA & $+3.7$ & \checkmark \\
Max input power & $\leq3.4$ & $P_{\mathrm{scr}}\approx+0.4$ & dBm & $-3.0$ & VOA required \\
Min OMA sensitivity & $\geq-6.0$ & $-17.1$ (shot-noise) & dBm & $+11.1$ & \checkmark \\
Required $i_n$ & $\leq18$ & $\approx0.65$ (PD only) & pA/$\sqrt{\text{Hz}}$ & $+17.4$ & \checkmark (TIA limited) \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\Cref{tab:ieee_compliance} reveals one compliance gap: the maximum permissible
input power of \SI{+3.4}{\dBm} exceeds the photodetector's field-screening onset
of $P_{\mathrm{screening}}\approx+0.4\,\mathrm{dBm}$, necessitating either a
VOA in the receive optical path or a redesign targeting higher $P_{\mathrm{screening}}$
through a UTC architecture (which raises the right-hand side of
\cref{eq:saturation_current_density} by $v_{\mathrm{d,sat},e}/v_{\mathrm{d,sat},h}=1.5$)
or wider mesa ($A_j\uparrow\Rightarrow P_{\mathrm{screening}}\uparrow$ via \cref{eq:p_screening}).

\subsection{Bandwidth and Modulation Format Implications for Upgrade Paths}
\label{subsec:pd_format}

The derived $f_{3\,\mathrm{dB}}\geq\SI{103}{\giga\hertz}$ accommodates
PAM-4 symbol rates up to approximately \SI{100}{\giga\baud}. The attenuation
of a sinusoidal component at the Nyquist frequency is:
\begin{equation}
    |H(f_N)| = \frac{1}{\sqrt{1+(f_N/f_{3\,\mathrm{dB}})^2}}.
    \label{eq:nyquist_attenuation}
\end{equation}
For PAM-4 at \SI{200}{\giga\bit\per\second}, corresponding to
$f_s=\SI{100}{\giga\baud}$ and $f_N=\SI{50}{\giga\hertz}$, the attenuation is
$|H(\SI{50}{\giga\hertz})|=1/\sqrt{1+(50/103)^2}\approx0.90$. This is less than
\SI{1}{\decibel} of Nyquist-frequency attenuation, confirming ISI-free reception
without equalisation from \cref{eq:H_pd}. For a hypothetical upgrade to
\SI{400}{\giga\bit\per\second} PAM-4, where $f_N=\SI{100}{\giga\hertz}$,
$|H(\SI{100}{\giga\hertz})|=1/\sqrt{2}\approx-\SI{3}{\decibel}$. The device is
then operating exactly at the $-3\,\mathrm{dB}$ boundary, so at minimum a
single-tap FFE becomes necessary. The minimum receiver sensitivity at
\SI{400}{\giga\bit\per\second} is further degraded by the equalisation noise
enhancement factor:
\begin{equation}
    \mathrm{NEF} \approx \sqrt{1+(f_N/f_{3\,\mathrm{dB}})^2}
    = \sqrt{2} \approx \SI{1.5}{\decibel}
    \quad\text{at }f_N=f_{3\,\mathrm{dB}}.
    \label{eq:nef}
\end{equation}
This noise enhancement penalty originates in the photodetector transfer function
and propagates through the equaliser into the receiver BER budget, providing the
direct quantitative connection between the device-level $f_{3\,\mathrm{dB}}$ of
\cref{eq:bw_total} and the system-level DSP complexity~\cite{Pilori2019}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.90\linewidth]{
        Four-panel system figure. Panel (a): receiver sensitivity
        $P_{\mathrm{sens}}$ (dBm) from \cref{eq:sens_full} versus electrical
        bandwidth $B_e$ from 10 to \SI{200}{\giga\hertz}, for three
        responsivity values: $\mathcal{R}=0.75,0.95,1.05$\,A/W.
        Fix $I_d=\SI{1.3}{\nano\ampere}$ and $Q=7.03$. Mark the operating
        point with a filled circle and the dark-current-dominated vs
        signal-dominated crossover from \cref{eq:sens_dark,eq:sens_signal}.
        Panel (b): required OMA$_{\mathrm{out}}$ (dBm) for BER$=10^{-4}$
        from \cref{eq:ser_pam} versus modulation order $M=2,4,8$ at symbol
        rates adjusted to maintain \SI{200}{\giga\bit\per\second} aggregate.
        Panel (c): normalised $|H(f)|^2$ (dB) from 1 to \SI{250}{\giga\hertz}
        with the present device ($f_{3\,\mathrm{dB}}=103$\,GHz), with vertical
        dashed lines at Nyquist frequencies for OOK/100G (50\,GHz),
        PAM-4/200G (50\,GHz), and PAM-4/400G (100\,GHz), annotating the
        Nyquist attenuation and NEF from \cref{eq:nyquist_attenuation,eq:nef}.
        Panel (d): compact receiver chain diagram showing fibre, coupler,
        waveguide, photodiode, TIA, equaliser, and BER monitor, with a lane-level
        annotation of where each loss or noise term from the chapter enters the
        system budget.}
    \caption{Device-to-system projection for the IEEE~802.3 400GBASE-DR4 link.
    Left: sensitivity vs bandwidth for three responsivity values. Centre: PAM-$M$
    OMA penalty from \cref{eq:ser_pam}. Right: photodetector frequency response
    with Nyquist frequencies for current and future standards, quantifying the
    equalisation burden via \cref{eq:nef}.}
    \label{fig:system_projection}
\end{figure}

\subsection{PIC-Level Integration and Layout Constraints}
\label{subsec:pd_pic}

Integration of the photodetector within a silicon photonic integrated circuit
(PIC) introduces layout constraints that modulate the effective device performance
beyond the isolated-device metrics of \cref{tab:results}. Four constraints are
quantitatively significant:

\paragraph{Waveguide routing attenuation.}
For a Si ridge waveguide with $\gamma_{\mathrm{wg}}=\SI{3.6}{\decibel\per\centi\metre}$
at \SI{1310}{\nano\metre} and a routing length of $\ell_{\mathrm{wg}}$,
the chip-level responsivity is:
\begin{equation}
    \mathcal{R}_{\mathrm{chip}} = \mathcal{R}\cdot10^{-\gamma_{\mathrm{wg}}\ell_{\mathrm{wg}}/10}.
    \label{eq:r_chip}
\end{equation}
For $\ell_{\mathrm{wg}}=\SI{5}{\milli\metre}$ (a typical intra-PIC routing
distance in a 4-lane transceiver)~\cite{Wang2024},
$\mathcal{R}_{\mathrm{chip}}=0.95\times10^{-0.0018}\approx\SI{0.946}{\ampere\per\watt}$---
a negligible \SI{0.2}{\percent} reduction.

\paragraph{Taper yield sensitivity.}
The adiabatic taper couples efficiency $\eta_c$ from the Si waveguide mode to
the Ge absorber. A lithographic overlay error of $\Delta W_{\mathrm{tip}}$ in
the tip width alters the coupling efficiency by
$d\eta_c/dW_{\mathrm{tip}}\cdot\Delta W_{\mathrm{tip}}$. FDTD sensitivity
analysis (not detailed here) shows that $\eta_c$ remains above \SI{95}{\percent}
for tip width variations of $\pm\SI{20}{\nano\metre}$---within the
$3\sigma$ tolerance of a \SI{90}{\nano\metre} lithography node.

\paragraph{Thermal cross-talk.}
In a 4-lane PIC, adjacent modulators dissipate several milliwatts of electrical
power, raising the local temperature of the photodetector by
$\Delta T\approx P_{\mathrm{mod}}\cdot R_{\mathrm{th}}$, where
$R_{\mathrm{th}}\approx\SI{50}{\kelvin\per\watt}$ is a typical SOI thermal
resistance. For $P_{\mathrm{mod}}=\SI{5}{\milli\watt}$, $\Delta T\approx\SI{0.25}{\kelvin}$,
increasing $I_d$ by $\approx\SI{3}{\percent}$---negligible for the present dark
current level.

\paragraph{Electrical parasitics from the bondpad.}
The bondpad capacitance $C_{\mathrm{par}}\approx\SI{10}{\femto\farad}$
contributes to $C_{\mathrm{tot}}=C_j+C_{\mathrm{par}}$ and therefore reduces
$f_{\mathrm{RC}}$. Co-design with a flip-chip-bonded TIA that eliminates the
bondpad and replaces it with a \SI{1}{\femto\farad} bump capacitance would
reduce $C_{\mathrm{tot}}$ by a factor of six, raising
$f_{\mathrm{RC}}$ from \SI{147}{\giga\hertz} to $\sim\SI{870}{\giga\hertz}$---
making the conservative baseline response entirely transport-limited, with the
remaining ceiling then set by the transit physics rather than by RC loading.
In other words, bondpad removal is highly valuable while the device is still
parasitic-limited, but it cannot by itself replace the need to understand the
two-carrier transport response or the package-level peaking network.

\section{Physics-Informed Neural Network: Validation and Double U-shape Extension}
\label{sec:pd_pinn}

\subsection{Motivation: Simulation Cost as the Design Bottleneck}
\label{subsec:pd_pinn_motivation}

The coupled FDTD-CHARGE simulation chain of \cref{sec:pd_design} requires
approximately \SI{11}{\hour} of wall-clock time per design point on a
shared-memory workstation with 32 cores. For the absorber length sweep of
\cref{subsec:pd_optical_results} (7 lengths) and the $W_i$ parameter sweep of
\cref{tab:wi_sweep} (7 thicknesses), the total simulation investment is
approximately $7\times\SI{3}{\hour}+7\times\SI{8}{\hour}=\SI{77}{\hour}$
of FDTD-only and CHARGE-only compute, respectively. A full 2-D parameter sweep
over both $L$ and $W_i$ simultaneously (say, $7\times7=49$ points) would require
approximately \SI{539}{\hour} (about 22 days)---computationally prohibitive
within a thesis timeline.

The exploration of an alternative electrode topology---the double U-shaped anode,
which wraps metal contacts around \emph{both} long sidewalls of the germanium
mesa, reducing the maximum lateral current path from $W_m/4$ to $W_m/8$---cannot
be addressed by the full FDTD-CHARGE chain within the available computational
budget. The PINN surrogate model provides the means to explore the double-U
design space at a fraction of the full-simulation cost, after first being
validated against the single-U reference design at selected bias points.

\subsection{PINN Architecture and Training Strategy}
\label{subsec:pd_pinn_arch}

The PINN employed here follows the loss formulation of \cref{eq:pinn_loss,eq:pinn_pde_loss}.
The network architecture consists of four fully connected hidden layers with 128
neurons each, hyperbolic-tangent activations, and a three-dimensional spatial
input $\mathbf{r}=(x,y,z)$ producing three outputs $(\hat{n},\hat{p},\hat{\phi})$.
The total number of trainable parameters is approximately $3\times128^2+3\times128\approx49\,000$,
small enough for training on a single GPU (NVIDIA A100) in approximately 4 hours.
The weighting hyperparameters $\lambda_{\mathrm{PDE}},\lambda_{\mathrm{BC}},\lambda_{\mathrm{data}}$
are tuned by a self-adaptive strategy that monitors the gradient magnitude of
each loss component and rescales weights to prevent any single term from
dominating early in training---a necessary precaution for the stiff PDE system
resulting from the $10^6$-fold variation in carrier density between the contact
layers and the depleted intrinsic region.

Collocation points are sampled by Sobol quasi-random sequences with $N_c=8192$
interior points and $N_{BC}=2048$ boundary points per training iteration.
The Ge/Si heterojunction is handled by including the graded transition layer
of $\delta_{\mathrm{grad}}=\SI{5}{\nano\metre}$ as a smooth variation in the
local band offset, avoiding the singularity in the quasi-Fermi level that would
otherwise prevent gradient computation through the AD graph.

\subsection{Validation Against the Single U-shaped Design}
\label{subsec:pd_pinn_valid}

The PINN is first trained and validated on the single U-shaped design at three
bias voltages $V=0,-0.5,-1.0$\,V. The key validation comparison is at
$V=\SI{-1}{\volt}$, where the CHARGE solution is anchored to the experimental
dark current benchmark of Shi~\textit{et al.}~\cite{Shi2024}. The PINN solution
is deemed validated only when three conditions are satisfied simultaneously.
\textbf{First,} the band diagram profiles $E_c(z)$ and $E_v(z)$ must agree with
CHARGE to within \SI{10}{\milli\electronvolt} RMS across the full device
thickness. \textbf{Second,} the carrier density profiles $n(z)$ and $p(z)$ must
agree with CHARGE to within \SI{15}{\percent} RMS within the intrinsic germanium
layer. \textbf{Third,} the integral dark current
$I_d=A_j\int_0^{W_i}U_{\mathrm{SRH,dep}}\,\mathrm{d}z$ must agree with CHARGE to
within \SI{5}{\percent}.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Four-panel PINN validation figure. Panel (a): band diagram comparison---
        CHARGE (solid lines) and PINN (dashed) for $E_c(z)$ and $E_v(z)$ at
        $V=\SI{-1}{\volt}$. Panel (b): carrier density comparison---$n(z)$
        (blue) and $p(z)$ (red), CHARGE (solid) vs PINN (dashed), on a
        logarithmic vertical axis. Panel (c): electric field magnitude
        $|\mathbf{E}(z)|$ comparison, CHARGE (solid) vs PINN (dashed), with
        the saturation threshold $E_{\mathrm{sat}}$ as a horizontal reference.
        Panel (d): PINN training loss curves ($\mathcal{L}_{\mathrm{PDE}}$,
        $\mathcal{L}_{\mathrm{BC}}$, and total $\mathcal{L}$) versus training
        epoch from 0 to 40\,000, on a logarithmic vertical axis, confirming
        convergence to residuals below $10^{-4}$.}
    \caption{PINN validation against the CHARGE drift-diffusion solution for
    the single U-shaped Ge-on-Si photodiode at $V=\SI{-1}{\volt}$. Agreement
    in band diagram (a), carrier density (b), and electric field (c) to within
    the criteria of \cref{subsec:pd_pinn_valid} confirms the PINN as a
    quantitatively reliable surrogate for exploring the double U-shaped design
    space.}
    \label{fig:pinn_validation}
\end{figure}

\subsection{Double U-shaped Anode: Concept and Expected Performance}
\label{subsec:pd_double_u}

The double U-shaped anode places metal contacts on both long sidewalls of the
germanium mesa, so that the maximum lateral current path in the $p^{++}$-Si
platform is halved relative to the single-U architecture:
\begin{equation}
    \ell_a^{\mathrm{2U}} \approx \frac{W_m}{8} + d_{\mathrm{gap}},
    \label{eq:la_2u}
\end{equation}
giving:
\begin{equation}
    R_{s,\mathrm{an}}^{\mathrm{2U}}
    \approx \frac{\rho_{\mathrm{Si}}}{t_{\mathrm{Si}}}
    \cdot\frac{W_m/8+d_{\mathrm{gap}}}{L}.
    \label{eq:rs_2u}
\end{equation}
For $W_m=\SI{5}{\micro\metre}$ and $d_{\mathrm{gap}}=\SI{0.5}{\micro\metre}$:
$\ell_a^{\mathrm{2U}}\approx\SI{1.125}{\micro\metre}$ versus
$\ell_a^{\mathrm{U}}\approx\SI{1.75}{\micro\metre}$, a further \SI{36}{\percent}
reduction. The predicted $f_{\mathrm{RC}}^{\mathrm{2U}}$ from \cref{eq:bw_rc}
with $R_{s,\mathrm{an}}^{\mathrm{2U}}\approx\SI{3.2}{\ohm}$ and
$C_j=\SI{1.8}{\femto\farad}$:
\begin{equation}
    f_{\mathrm{RC}}^{\mathrm{2U}}
    = \frac{1}{2\pi(50+3.2)\times1.8\times10^{-15}}
    \approx\SI{165}{\giga\hertz},
    \label{eq:frc_2u}
\end{equation}
and the combined bandwidth from \cref{eq:bw_total}:
\begin{equation}
    f_{3\,\mathrm{dB}}^{\mathrm{2U}}
    = \left(\frac{1}{51.4^2}+\frac{1}{165^2}\right)^{-1/2}
    \approx\SI{50}{\giga\hertz}.
    \label{eq:bw_2u}
\end{equation}
The double-U yields only a marginal improvement in the conservative,
no-peaking baseline ($\SI{50}{\giga\hertz}$ versus $\SI{44.5}{\giga\hertz}$
for the single-U at $W_i=\SI{350}{\nano\metre}$) because the device is already
transport-dominated in that lower-bound model: once $R_s$ has been strongly
reduced, further resistance reduction buys progressively less bandwidth. This
does not contradict the \SI{103}{\giga\hertz} single-U benchmark discussed
earlier, because that value includes the benchmark-calibrated two-carrier
transport response and inductive peaking of \cref{eq:H_pd_pk}, neither of which
is re-optimised in the present double-U exploration. The principal motivation
for exploring the double-U is therefore not to over-claim a new absolute
bandwidth record, but to test whether further electrode engineering can
materially change the underlying RC-versus-transport balance before a more
expensive full RF re-optimisation is warranted.

\begin{figure}[H]
    \centering
    \missingfigure[figwidth=0.85\linewidth]{
        Four-panel double-U PINN results. Panel (a): top-view schematic of
        the double U-shaped electrode layout (metal wrapping both long sidewalls
        of the Ge mesa), with current streamlines in the $p^{++}$-Si platform
        showing the halved path $\ell_a^{\mathrm{2U}}=\SI{1.125}{\micro\metre}$.
        Panel (b): electric field magnitude $|\mathbf{E}(z)|$ at $V=\SI{-1}{\volt}$
        from the PINN solution for the double-U design (solid) versus single-U
        CHARGE reference (dashed), confirming field uniformity in the intrinsic
        layer is preserved. Panel (c): lateral current-density or potential map
        in the $p^{++}$-Si platform comparing single-U and double-U collection
        paths, making the resistance reduction visually explicit. Panel (d): $f_{3\,\mathrm{dB}}$ (GHz) versus
        $R_s$ (\si{\ohm}) from 0 to \SI{15}{\ohm}, computed from
        \cref{eq:bw_total} with the conservative analytic model, showing four
        design points: parallel (\SI{43}{\giga\hertz}), single-U
        (\SI{44.5}{\giga\hertz}), double-U (\SI{50}{\giga\hertz}), and the
        lower-bound transit ceiling (\SI{51.4}{\giga\hertz}). Add a separate
        annotation noting the benchmark-calibrated single-U response at
        \SI{103}{\giga\hertz}.}
    \caption{Double U-shaped anode exploration via the validated PINN surrogate.
    The halved lateral path length from \cref{eq:la_2u} reduces $R_{s,\mathrm{an}}$
    by a further \SI{36}{\percent}, but the marginal $f_{3\,\mathrm{dB}}$
    improvement from the single-U value confirms that, within the conservative
    geometry-only model, the vertical $W_i=\SI{350}{\nano\metre}$ architecture
    is already close to its RC-versus-transport knee.}
    \label{fig:double_u_pinn}
\end{figure}

\paragraph{The double U-shape as a research contribution.}
The PINN-based exploration of the double-U architecture represents the original
research contribution of this chapter that distinguishes it from a reproduction
of the Shi~\textit{et al.}~\cite{Shi2024} benchmark. The contribution is
three-fold: (i) the PINN surrogate is validated at device level against a
state-of-the-art experimental benchmark, establishing its credibility as a
design tool; (ii) the double-U simulation identifies the point of diminishing
returns for further electrode reduction within the conservative
vertical-geometry model, complementing the analytical prediction of
\cref{eq:bw_tt}; and (iii) the computational cost comparison---\SI{4}{\hour}
for PINN versus $>\SI{8}{\hour}$ per CHARGE run---quantifies the practical value
of the surrogate approach for future design-space exploration, including
alternative electrode topologies (triple-U, split-cathode, wrap-around cathode)
that are precluded by the full-simulation cost budget.

\section{Conclusion}
\label{sec:pd_conclusion}

This chapter has developed a physically rigorous, simulation-driven
characterisation of a vertical n-i-p Ge-on-Si waveguide photodiode for
O-band reception at \SI{1310}{\nano\metre}, structured as a complete engineering
guide in which every design decision is traceable to a specific equation or
physical argument. The chapter first established the electromagnetic, material,
transport, and metric framework required to judge the device rigorously. It
then used a staged device-evolution argument together with the literature
review to show why the waveguide-integrated Ge-on-Si PIN remains the correct
architecture for the present target: germanium supplies the required O-band
absorption while preserving silicon-photonics process compatibility, the PIN
structure suppresses the diffusion tail associated with simpler junctions, and
waveguide coupling decouples absorption length from depletion thickness in a
way that surface-normal illumination cannot.

The analytical framework of \cref{sec:pd_concept} derived all performance
metrics from first principles: the Beer-Lambert absorption coefficient from
\cref{eq:alpha_kappa}, the drift-diffusion transport from
\cref{eq:cont_n_transient,eq:cont_p_transient}, the analytical photocurrent
transfer function from \cref{eq:jt_analytical}, and the optimal intrinsic-layer
thickness from \cref{eq:wi_optimal}. The electrode topology comparison of
\cref{subsec:pd_electrode} proved quantitatively that the single-U anode reduces
$R_{s,\mathrm{an}}$ by \SI{36}{\percent} relative to the parallel-electrode
design (\cref{eq:rs_ratio}) and, more importantly, moves the device into a
far more favourable electro-optical operating point once the peaking network of
\cref{eq:H_pd_pk} is co-optimised with the reduced series resistance.

The three-dimensional FDTD and CHARGE simulation chain reproduced the static and
optical benchmark metrics relevant to the O-band adaptation:
$\mathcal{R}\geq\SI{0.95}{\ampere\per\watt}$ and
$I_d=\SI{1.3}{\nano\ampere}$ at \SI{-1}{\volt}. The high-speed response was then
positioned using two complementary models: the conservative baseline
\cref{eq:bw_total}, which yields $f_{3\,\mathrm{dB,base}}\approx\SI{44.5}{\giga\hertz}$
for the compact O-band geometry, and the benchmark-calibrated peaked response
\cref{eq:H_pd_pk}, which places the validated single-U device in the
\SI{103}{\giga\hertz} class. The absorber length sweep of
\cref{subsec:pd_optical_results} and the $W_i$ parameter sweep of
\cref{tab:wi_sweep} quantified the trade-off surface of the vertical
architecture at a rate of approximately \SI{2.8}{\giga\hertz} per
\SI{1}{\percent} of responsivity sacrificed in the baseline screening model.

The device-to-system projection of \cref{sec:pd_system} derived the shot-noise-limited
receiver sensitivity of $P_{\mathrm{sens}}\approx\SI{-17.1}{\dBm}$ from
\cref{eq:sens_full} and established full compliance with the IEEE~802.3
400GBASE-DR4 standard (\cref{tab:ieee_compliance}) with one exception: the
maximum input power of \SI{+3.4}{\dBm} exceeds the electric field screening
onset of \cref{eq:p_screening}, requiring a VOA in the receive path or a UTC
architecture to extend the linear dynamic range.

The PINN surrogate of \cref{sec:pd_pinn}, validated to within \SI{5}{\percent}
on dark current against the CHARGE reference, enabled the double U-shaped anode
to be explored at a fraction of the full-simulation cost. The double-U result
$f_{3\,\mathrm{dB}}^{\mathrm{2U}}\approx\SI{50}{\giga\hertz}$ confirms that in
the conservative geometry-screening model, the vertical
$W_i=\SI{350}{\nano\metre}$ architecture already sits close to its practical
RC-versus-transport knee. In other words, once the single-U has removed the
dominant parasitic bottleneck, further electrode engineering alone yields only
marginal improvement unless the transport physics, the peaking network, or the
junction geometry itself is also changed.

Three specific open questions require future experimental address: (i) the direct
measurement of $\tau_0$ in as-grown Ge-on-Si epitaxial layers through time-resolved
photoluminescence, to validate or refine the calibrated SRH lifetime assumption;
(ii) the extraction of the parasitic bondpad capacitance $C_{\mathrm{par}}$ from
on-wafer S-parameter measurements, to separate it from $C_j$ in the bandwidth
model and enable a quantitative prediction of $f_{\mathrm{RC}}$ without relying
on the modelled $R_s$; and (iii) the experimental fabrication and S-parameter
characterisation of the double U-shaped electrode device to validate the PINN
prediction of the conservative baseline response and determine whether a
retuned peaking network can convert that geometrical change into a meaningful
measured RF improvement.

\paragraph{Industry and research directions.}
The present work positions itself at the intersection of three converging trends
in high-speed silicon photonics. First, the industry drive toward
\SI{800}{\giga\bit\per\second} and \SI{1.6}{\tera\bit\per\second} per-lane rates
(IEEE~802.3 Clause~139 and beyond) requires per-lane symbol rates of
\SI{200}{}--\SI{400}{\giga\baud}, which are fundamentally inaccessible to the
vertical PIN architecture at $W_i=\SI{350}{\nano\metre}$ and will require either
lateral Ge-fin geometries with $W_{\mathrm{Ge}}\leq\SI{100}{\nano\metre}$
(where the Monte Carlo overshoot effect of \cref{sec:pd_discussion} becomes
advantageous) or III-V-on-Si heterogeneous integration for UTC designs.
Second, the emergence of foundry-validated PINN-based device simulation tools
suggests that the computational bottleneck identified in \cref{subsec:pd_pinn_motivation}
will be progressively removed, enabling systematic multi-parameter co-optimisation
of electrode geometry, absorber dimensions, and doping profile within a single
automated pipeline. Third, the integration of nano-optical absorption enhancement
structures (GMR gratings, photonic crystals as in \cref{sec:pd_discussion})
with the waveguide-coupled PIN geometry offers a path to simultaneously improving
$\mathcal{A}$, reducing $A_j$, and raising the $P_{\mathrm{screening}}$ limit---
potentially enabling a single device architecture to satisfy both the
400GBASE-DR4 and the emerging 800GBASE and 1.6TBASE standards without
architectural changes. The physical intuitions, analytical inequalities, and
simulation methodology developed in this chapter constitute the engineering
framework for navigating these directions.


